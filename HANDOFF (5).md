# 📦 HANDOFF کامل و یکجا — پروژهٔ «دستگیر / بازارچه» · تا نسخهٔ v175
نوشته‌شده در ۲۰۲۶-۰۹-۰۵، پایان چتِ «ممیزی جامع پنل (فازهای ۰–۱۵)».
> **این فایل برای چیست:** همهٔ دانش همکاری از اول تا الان، در یک فایل خودکفا — تا در چت جدید هیچ سؤال‌وجوابی از صفر شروع نشود.
> **طریقهٔ استفاده در چت جدید:** این فایل را بده + بگو «بخوان و ادامه بده». پیوستِ انتهای فایل، دانش‌نامهٔ کامل نسل دوم (دورهای ۱–۱۹) است که عیناً حفظ شده.

---

## الف) وضعیت همین لحظه (مهم‌ترین بخش)

- **آخرین تحویل: `dastgir-v175.zip`** — md5 `dfc926cc0091e9cd253530a10e0c4e92` · ۴٬۶۹۷٬۳۹۵ بایت · ۴۴۵ فایل · بوت‌تست 200 (عنوان «خانه | دستگیر»). v174.zip پس از تحویل حذف شد.
- نشانگرها: `VERSION.txt`=175 · `site/sw.js`=`bazarche-v175` · درگاه v175 بالای `WHATS-NEW.md`.
- **ممیزی جامع پنل مدیریتی تمام شد: فازهای ۰ تا ۱۵ همه ✅** — دفترچهٔ کامل با مدرک: `work/dastgir/QA-panel-audit-plan.md`.
- رگرسیون پایانی: **۳۹۴ تست سبز** (۱۳۱ پایه + ۲۶۳ فاز) + خزندهٔ ۴ نقش ۲٬۷۷۵ صفحه، صفر غیر-200 + وارسی ۱۰بندی صحنه سبز.
- مسیرها در ورک‌اسپیس:
  - `/home/user/dastgir-v175.zip` — تحویل فعال
  - `/home/user/RULES.md` — قوانین ثبت‌شدهٔ همکاری
  - `/home/user/work/dastgir/` — **نسخهٔ کاری v175** (کل کد سایت؛ `python3 run.py` → پورت 8080)
  - `/home/user/work/dastgir/QA-panel-audit-plan.md` — دفترچهٔ ممیزی
  - `/home/user/work/dastgir/tools/qa_*.py` — ۱۶ باتری آزمون
  - `/home/user/work/dastgir/qa/*.json` — نتایج باتری‌ها
  - `/home/user/repo/` — کپی ریپو + `dastgir-v152.zip` (نقطهٔ شروع مسئولیت من) + `FULL-HANDOFF (1).md` (نسل دوم — همان پیوست انتهای این فایل)
  - `/home/user/vps-comparison-fa.md` — مقایسهٔ VPS (مرجع)
- سرور تستِ این چت روشن بود (`v175-cand-5a19b3c8`، پورت 8080) — در چت جدید نیست؛ `cd work/dastgir && python3 run.py` کافی است.

---

## ب) تاریخچهٔ کامل — سه نسل

### نسل ۱–۲: ممیزی‌های قدیمی تا v117 (دورهای ۱ تا ۱۹)
- کل این دوره **عیناً در پیوست انتهای همین فایل** است: معماری، ۵۷ جدول، دفتر یافته‌های F-001…F-035، بنچمارک‌ها، استانداردهای UX، نقشهٔ کد، روش انتشار.
- برجسته‌ها: کش صفحات مهمان با اثرانگشت محتوا (F-030/031) · ۷ ایندکس داغ (تا ۵۱۰×) · PBKDF2 600k · سپر ضد-اسپم shield.py · GeoIP فارسی ۹۷٪ · سشن دو-پلهٔ پنل · ۳۹ سوئیت test_* · رمبش ۱۰۰۰ کاربر ۵۰۰۰/۵۰۰۰.
- **تصحیح مسیرها نسبت به پیوست:** ورک‌اسپیس آن دور `/home/user/repo-369/site/` بود؛ الان همه‌چیز در `/home/user/work/dastgir/` است. نسخه و نشانگرها عوض شده (بخش الف). `audit/` و `shots/` آن دور دیگر موجود نیستند.

### نسل ۳: v117 → v152 (خلاصهٔ موجود)
- جزئیات این دورها در چت‌های قبل بود و کامل به این فایل منتقل نشده؛ قابل بازسازی از WHATS-NEW داخل زیپ‌ها.
- نکات ثبت‌شده: تغییر نام سراسری «مغازه»→«دکان» · حذف کامل سیستم وفاداری (امتیاز/کوپن/تبادل — جدول‌ها می‌مانند، مسیر زنده ندارند) · چت تلگرامی با پیوست فقط-عکس · پشتیبانی فقط تیکت (تماس/واتساپ حذف) · استوری ویدئویی ۳۰ثانیه‌ای · نوار پایین شیشه‌ای موبایل · حذف اسپرایت/ویدئوی سنگین هیرو (LCP).
- v141 آخرین نسخهٔ پیش از دور فعلی بود. **v152** (آپلود کاربر در ریپو، ۲۰۲۶-۰۹-۰۵ ۱۱:۱۰ تهران، `main @ 239b2bf`) = «بخش ۷ فاز ۱: شش اصلاح بازبینی کاربر» — نقطهٔ شروع مسئولیت من (نشانه‌ها: sw `bazarche-v152`، ui `?v152`، 15-PASS 0-FAIL).

### نسل ۴ — دور فعلی: v152 → v175 (همه‌چیز)
1. **v156 — بخش ۶ + ۷ (کار دو-بخشی کاربر):**
   - بخش ۶ نقشه: پین کوچک‌تر + کوچک‌ترشدن *واقعی* با زوم در ۴ حالت؛ فرمول مقیاس هر دو موتور `min(1.1, max(0.42, 0.87^(z−14)))` روی عنصرِ داخل divIcon (نه ریشه).
   - بخش ۷ فراخوان دکان: دسترسی با هر سه مسیر هم‌ارز (پرک `broadcast` پلن · بنیان‌گذاران اولین-N `settings.founder_limit` · گرهٔ دستی مدیر biz_features + نشان «بنیان‌گذار»). «موجود شد خبرم کن» از صفحهٔ کالای ناموجود + درون گفتگو. رساندن **فقط** زنگولهٔ داخل سایت (kind="broadcast"، push=False، احترام به کلید ۸م «فراخوان‌ها»). درخواست باز تکراری رد می‌شود؛ پس از fulfilled/dismissed آزاد. سقف ۱۰ پیام/ساعت برای هر مالک. پشتیبان JSON/زیپ جدول‌های تازهٔ broadcasts/stock_requests را می‌برد.
2. **v157:** درس‌های UX (ردیف فلکسی با دکمه در موبایل یا wrap یا دکمه flex:none).
3. **v158:** شمارهٔ تلفن دکان همیشه = شمارهٔ ورود مالک (فرم readonly، ارسالی نادیده).
4. **v160–v165:** اصلاحات بازبینی + یافته‌های ۱۳–۱۶ (یکدست‌سازی قالب تاریخ با فاصله — ریشهٔ بعدی #23؛ دو باگ خفته از همین نسخه: فرم قابلیت‌ها که بعداً #30 شد و کلید بی‌اثر blog_enabled که #34 شد). v164 آپلود آزمایشی x0.at (/879009d4).
5. **دور ممیزی جامع پنل (فرمان کاربر: «همهٔ آیتم‌های منو + دسترسی‌ها، تک‌به‌تک، اول شناخت بعد باگ/ارتقا/امنیت؛ فازها عملیاتی و ریز، چند بخش در هر فاز»)** — مصوب در `QA-panel-audit-plan.md`؛ قواعد هر فاز: شناخت → باتری تازه → رفع/رد → دفترچه؛ پایان هر دور: رگرسیون + وارسی صحنه + بامپ/زیپ/تحویل + حذف زیپ قبلی:
   - **v167 — فاز ۱** (داشبورد/اینباکس/تعدیل): ارتقای A2 (۳ شمارندهٔ معوق) + **#17** (گروه گزارش تخلف هرگز در اینباکس نمی‌آمد — فیلتر `new` درحالی‌که وضعیت واقعی `open` است).
   - **v168 — فاز ۲** (سفارش‌ها/بینش/جست‌وجو): ارتقای A3 (سفارش‌ها در جست‌وجوی پنل) + **#18 بحرانی** (برچسب نتایج جست‌وجو بدون esc → XSS ذخیره‌شده علیه نشست ادمین؛ اثبات با پیلواد onerror).
   - **فازهای ۳–۴** (گفت‌وگوها/تیکت‌ها · کسب‌وکارها/دسته‌ها/محله‌ها): تحویل در همین بازه؛ **#19** ابزارخودی (reset_testdata دکان‌های پایه را کشت → بازسازی صحنه با شناسه‌های تازه 214/215/216 + وصلهٔ KEEP_SLUGS/KEEP_OWNERS).
   - **v170 — فازهای ۵+۶** (برترین‌ها/پیشنهاد/استوری · رویداد/مقاله): A4 واقعی (reset_testdata با KEEPها + اثبات دو اجرا) · A5 (صفحهٔ ویرایش مقاله `/panel/post/{id}` و رویداد `/panel/event/{id}`) · **#20** (رویداد وحشی → رکورد یاچی/حذف جعلی) · **#21** (مقالهٔ بی‌عنوان + حذف جعلی) · **#22** (خودم: پاک‌سازی کاندید، رویداد واقعی یلدا را کشت → بازسازی، id تازه=12) · **#23** (فیلتر سیل `ends>=now` با isoformat/T مقایسه می‌شد → سیلِ امروز هرگز در /deals دیده نمی‌شد — رگرسیون خودِ یافتهٔ ۱۵).
   - **v171 — فازهای ۷+۸** (نظرات/پرسش/گزارش · نوبت‌ها/پیام‌ها/هشدار): **#24** (ثبت نوبت عمومی کاملاً بی‌اعتبارسنجی: خالی/بی‌شماره/نوبت‌خاموش/بی‌نرخ‌سنج) · **#25** (وضعیت نوبت بدون whitelist) · **#26** (خانوادهٔ ۹مسیرهٔ «وحشی→جعل موفقیت»: نظر/پرسش/گزارش/پیام/هشدار/نوبت پنل + پاسخ نظر و وضعیت نوبت دکاندار + دلیل جعلی گزارش + helpful ناموجود).
   - **v172 — فازهای ۹+۱۰** (صاحبان/تصاحب/تغییرات/نشان · پلن/قابلیت/اشتراک/تبلیغ): **#30 بحرانی** (هر ذخیرهٔ «کلید قابلیت‌ها» همهٔ ۲۵ کلید را خاموش می‌کرد — form.get خام روی لیست parse_qs؛ از v165 هیچ ذخیرهٔ موفقی نبود!) · **#27** (۱۱مسیر وحشی دیگر: owner/claim/edit/badge×3/plan/subscription×4 + پلن ناموجود با ok + بنر×3) · **#28** (بازگردش بنر `&`→`?`) · **#29** (تصاحب دکان ناموجود → FK 500) · قرارداد: months اشتراک فقط 1/3/6/12 · **#31** (خودم: سه قرمز اول فاز ۹ صحنهٔ خودم بود — درس: پیش‌فرض صحنه را از پایگاه بخوان).
   - **v173 — فازهای ۱۱+۱۲** (محتوا/صفحات ثابت/ظاهر/فلگ · رسانه/مشتری/دنبال‌کننده/اعلان): **#34** (کلید blog_enabled از v165 هیچ‌جا خوانده نمی‌شد — خاموشش /blog را نمی‌بست؛ گارد در GET /blog و /blog/{slug}) · **#33** (block/unblock/delete مشتری وحشی «موفق» جعل می‌کرد) · **#32** (بازگردش ظاهر `&`→`?`). مدرک فاز ۱۱: رنگ سالم→CSS روز، رنگ زباله→پیش‌فرض برند بدون تزریق CSS، لوگو آپلود/حذف، XSS صفحات/هیرو escaped، دکاندار 403. مدرک فاز ۱۲: مسیرخارج رسانه ×2 بی‌خطر، مسدودی مشتری = کشتن سشن + سفارش بی‌اثر، حذف مشتری = ۱۱ جدول ردپا، broadcast فقط فعال‌ها + لینک فقط داخلی + متن escaped.
   - **v174 — فازهای ۱۳+۱۴** (فعالیت‌ها/ترافیک/آمار · سطل/تنظیمات/پشتیبان/نمونه): **#35** (بازیابی وحشی سطل «موفقیت» جعل می‌کرد) · **#36** (برداشتن مسدودی وحشی سپر همینطور) · **#39 مهم** (ریشهٔ «database is locked»: WAL تا چندMB رشد می‌کرد و هر commit چک‌پوینت سنگین را با قفل نویسنده می‌پرداخت — روی حافظهٔ گوشی حاد؛ رفع: چک‌پوینت پیش‌گیرانه در request_done وقتی WAL>1.5MB؛ اثبات: ۳۹ پنجرهٔ قفل→۰، نوشتن بیرونی بعد از seed 0.00s) · **#37** (خودم: باتری با demo.clear بنر بذر id=8 را کشت — ذاتاً تگ‌دار است! بازسازی + تعویض کنش آزمون به features.save) · **#38** (خودم: اتصال دومِ کامیت‌نشده در finally باتری با خودش بن‌بست می‌شد → q() استثنا-امن با retry). مدرک: ZIP پشتیبان با manifest sha256 درست، اسنپ‌شات SQLite، ماسهٔ سه‌حالتهٔ کلیدها، FCM نامعتبر→err، seed دقیقاً ۱۰ + دوباره‌کاری بی‌انبارش + clear بی‌ردپا.
   - **v175 — فاز ۱۵ (پایانی)**: **#40** (سبد دستکاری‌شده: item_id ناموجود → FK 500؛ و item_id واقعیِ دکان دیگر بی‌صدا به سفارش می‌چسبید → هر دو به ردیف آزاد item_id=NULL؛ رگرسیون F-012 سبز) · **ارتقای دِدوپ صف تغییرات** (ارسال تکراری همان diff → ردیف تازه نه؛ صف تمیز می‌ماند) · **حکم مستند: دِدوپ helpful رد شد** (سیگنال اجتماعی + نرخ‌سنج ۴۰/پنجره؛ دِدوپ درست = نگهداری هویت رأی‌دهنده — هزینه > فایده) · **#41 #42** (خودم: نام دکان 214 برنمی‌گشت؛ ident قدیمی و آستانهٔ آماری صحنه‌محور → نسبت وزن‌ها ≥2.2:1).

---

## ج) قوانین و خط‌قرمزهای کاربر (عیناً — همیشه معتبر)

1. ❌ **دروغ هرگز** — هیچ «درست شد» بدون مدرک واقعی: curl+کد وضعیت / exit code / چک دیتابیس / سنجهٔ DOM. خبر بدِ صادقانه بهتر از خبر خوبِ دروغین (کاربر راست‌گویی را تحسین می‌کند).
2. ❌ تنبلی/میان‌برد · ❌ شتاب‌زدگی («دقت بر سرعت مقدم») · ❌ بی‌دقتی — **لوپ ۲۰ بار**: بعد از تمام‌شدن کار دوباره از اول دنبال اشکال واقعی بگرد · ❌ خروجی بدون تحقیق.
3. **موبایل‌اول:** عرض ۳۲۰–۴۱۲ (و ۷۶۸)، هدف لمسی ≥44px، فونت بدن 14، هیچ متن غیرضروری زیر 12px، بدون سرریز افقی، RTL کامل با inset-inline.
4. **قانون ۱ (پرامپت دقیق‌شده):** قبل از اجرا، بازنویسیِ کوتاه کار را با قالب هدف/دامنه/معیار پذیرش/خط‌قرمزها/روش تأیید/خروجی نشان بده.
5. **قانون ۲ (پیام بلند):** اول برنامهٔ N بخشی (تعداد بخش = تعداد دستور، بدون ادغام)؛ هر پیام یک بخش؛ تیک ⏳→✅ و برنامهٔ به‌روز. اگر جوابی نیامد از بخش ۱ شروع کن و منتظر نمان.
6. **قانون ۳:** شروع هر چت = اعلام وضعیت ریپو (`git ls-remote` + مقایسه با ورک‌اسپیس + گزارش یک‌خطی). منبع حقیقت = ریپوی گیت‌هاب `github.com/rezadb369-wq/369` (برنچ main؛ فعلاً `main @ 239b2bf` = v152 آپلود کاربر). پوش از سندباکس ممکن نیست — آپلود زیپ کارِ خود کاربر است.
7. **قانون ۴:** قاعده/سلیقهٔ تازه همان لحظه در RULES.md ثبت شود («ثبت شد»). قانون ثبت‌نشده = فراموش‌شده.
8. **قانون ۵:** بدون پرکننده (سبک Stop Slop جذب‌شده: بدون قید زائد، عینی، بدون جملهٔ شعاری — روی فارسی هم).
9. **قانون ۶:** بازسازی به‌جای وصله — راه‌حلی که تست را رد کرد کلاً از نو با دانش بیشتر.
10. **قانون ۷:** تم رسمی (خاکستری خنثی + یک رنگ برند؛ بدون هاله/گرادیان/بلور). رنگ جدید فقط توکن معنایی در tokens.css با کنتراست AA در **هر دو تم**؛ contrast_audit صفر خطا. تلهٔ معروف: `--color-muted` (سطح) ≠ `--color-muted-foreground` (متن).
11. **اجازه نپرس** — کار را بکن (استثنا: پیام چنددستوری → قانون ۲ مقدم است).
12. **هر تحویل = زیپ کامل تمیز** بدون data/ · raw/ · shots/ · qa/ · __pycache__ · *.db* · لاگ — و «برای هر فاز تمام‌شده فایل کامل پروژه تحویل بده». **زنجیرهٔ پاک:** بعد از تحویل جدید، زیپ قبلی از ورک‌اسپیس حذف شود.
13. تغییر در فایل‌های سرور/asset → بامپ هر سه با هم: `?vNNN` در ui.py + `VERSION` در sw.js + نگهبان نسخه.
14. کاربر توسعه‌دهنده نیست؛ فقط با گوشی اندروید کار می‌کند (Termius)؛ دستور ترمینال = تک‌خطی `root@130.185.78.237`. توضیح ساده و صادقانه، بدون اصطلاح سنگین. با اسکرین‌شات گوشی گزارش می‌دهد.
15. «کیفیت و امنیت و سرعت مهم، نه ارزانی.» امنیت اولویت همیشگی.
16. بازبینی سه‌نقشی (مشتری/دکاندار/مدیر) در هر تغییر UX.

---

## د) قراردادهای محصول (تصمیم‌های قطعی — برنگردان مگر کاربر بخواهد)

- الگوی دیوار: شهر/استان، چت درون‌برنامه‌ای، علاقه‌مندی، عکس‌دار. ویرایش دکان فقط از پنل.
- **وفاداری/امتیاز/کوپن حذف شده** (جدول‌های قدیمی بی‌مسیر زنده‌اند).
- **چت فقط عکس** (پیوست غیرعکس ممنوع) · **پشتیبانی فقط تیکت** (تماس/واتساپ پشتیبانی حذف).
- «موجود شد خبرم کن» = چت + صفحهٔ کالا؛ رساندن = فقط زنگولهٔ داخل سایت (بخش ۷).
- نظر = کادر امتیاز ۶ستونه (multicriteria با crit_*؛ v160-fix: form_get هر دو شکل را نرمال می‌کند).
- تلفن فرم دکان همیشه = شمارهٔ ورود مالک.
- رأی «مفید بود» عمداً دِدوپ تکی‌رأی ندارد (حکم v175 — بالا).
- قیمت کالا در نصب تازه خاموش (`prices_enabled=0`؛ /prices خاموش = 404).
- قالب تاریخ‌ها در DB یکدست ۱۹نویسه‌ای با فاصله (نه T) — ریشهٔ #23.
- شمارهٔ تلفن دکان = شمارهٔ ورود مالک.
- هیرو CSS-محور بدون تصویر/ویدئوی سنگین (LCP) — برنگردان مگر درخواست صریح.
- WebP برای آپلودی‌ها ممکن نیست (صفر-وابستگی) — شکاف شناخته‌شده؛ آینده: srcset.
- بازدید واقعی سرورسمت (هر HTML 200 = یک سطر visits؛ urllib بی‌UA = bot=1 — درست است).

---

## هـ) مرجع فنی کارِ روزمره

### محیط
- سرور تست: `cd /home/user/work/dastgir && python3 run.py` (پورت 8080؛ اگر اشغال بود find_free_port). سلامت: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8080/healthz`.
- کلید پنل این نصب: `python3 -c "import sys;sys.path.insert(0,'server');import db;print(db.owner_token())"` → ورود: GET `/panel?key=<token>`.
- **هر edit سرور = ری‌استارت پروسه** (auto-reload ندارد) — اول import-check بعد stop/start (با stop_process نه pkill عام — تلهٔ pkill: الگو در cmdline خودِ bash هم می‌آید).
- سرور بین نوبت‌ها/چت‌ها می‌میرد — عادی است.
- Playwright در `.cache` است که snapshot نمی‌ماند — بازیابی: `python3 -m playwright install --with-deps chromium`.
- تلهٔ pipe/grep: کرش وسط اسکریپت بی‌صدا می‌ماند — همیشه exit code بگیر.
- فایل‌های خارج از /home/user ماندگار نیستند؛ بوت‌تست موقت در /tmp و بعدش پاک+بستن پورت.

### دیتابیس و تله‌ها (مهم‌ترین‌ها؛ کامل نسل دوم در پیوست + بخش ۸ آن)
- `data/bazarche.db` — WAL. `db.connect()` row_factory نمی‌پذیرد · `PRAGMA foreign_keys=ON` · جدول audit است (نه audit_log)؛ `db.log(action,detail)`.
- ستون‌های پرخطا: `points.amount` · `customer_sessions(token,customer_id,expires)` · `notifications(customer_id,kind,text,link)` چهارستونه · order_items.item_id nullable با FK به items.
- مسیرهای فقط-multipart: `/panel/ad/new` و `/panel/appearance` (لوگو) — POST urlencoded بی‌اثر.
- features ذخیرهٔ **کل‌وضعیتی** است (POST جزئی → بقیه خاموش؛ whitelist کلید) — #30 رفع‌شده ولی همیشه با form_get بخوان.
- POST پنلی بدون گیتِ ?key → 403؛ باتری‌ها GET `/panel?key=DB.owner_token()` کنند. پیش از ورود آزمونی: `delete from ratelimit; delete from otp`.
- «database is locked» با هر دو پروسهٔ بیکار = تراکنشِ پارک‌شدهٔ خودی (اتصال دومِ کامیت‌نشده). الگوی q() مقاوم (finally+retry) در `tools/qa_phase15.py` — کپی کن. رفع محصولی #39 (چک‌پوینت پیش‌گیرانهٔ WAL) از v174 هست.
- پاک‌سازی آزمون فقط با id/نشانهٔ دقیق خود آزمون · `reset_testdata` وصله‌خورده (KEEP_SLUGS/KEEP_OWNERS) · **`wipe_all --all` مخرب — اجرا نشود.**
- seed_demo: برچسب «نمونه‌آزمایشی» + **بنر بذر id=8 ذاتاً تگ‌دار است — هر clear نابودش می‌کند** (بازسازی: INSERT با id صریح 8؛ شکل در باتری فاز ۱۴).

### صحنهٔ آزمون پایه (وارسی ۱۰بندی — همه باید سبز بماند)
- ادمین `09131110011` (owner 1) · دکاندار A `09352223344` (owner 2؛ دکان 214 «دکانٔ آزمون مالک یک» slug `dkan-azmvn-malk-yk`) · دکاندار B `09134950001` (owner 89؛ دکان 215) · مشتری `09361112233` (customer 17).
- مشتریان صحنه فقط idهای 1,2,3,4,5,7,8,17 · مالک‌ها فقط 1,2,86,89.
- دکان‌های 214/215/216: پایان دور `owner_id=NULL, booking_on=0` · دکان 214 کالای 336 «کالای آزمون 1» با `available=1` (سفارش آزمون موجودی را صفر می‌کند — پیش از qa_customer برگردان).
- بذر نظر پنل: یک نظر `author='مشتری آزمون'` روی 214 (ستون‌های reviews: id,biz_id,author,rating,body,reply,image,status,customer_id,created,helpful) — **qa_panel و صحنه به آن نیاز دارند؛ qa_shop و phase15 بعد از خودشان حذفش می‌کنند — پیش از اجرای باتری‌های دیگر برش گردان** (INSERT نمونه در بخش هـ صحنهٔ آزمون همین فایل و در بدنهٔ qa_panel/phase15 هست).
- رویداد بذر cityevent id=12 · مقالهٔ بذر post id=8 · بنر بذر id=8 · `edit_approval=0` · همهٔ `*_enabled`=1 · پایان کار: `otp`, `ratelimit`, `searches` خالی.

### باتری‌ها (۱۶ فایل در tools/ · نتایج در qa/)
- پایه: qa_shop **30** · qa_customer **27** · qa_panel **36** · qa_security **22** · qa_mobile **16** · qa_crawl (خزندهٔ ۴ نقش anon/customer/shop/owner — آخرین اجرا ~۲٬۷۷۵ صفحه صفر غیر-200).
- فازها: phase5=23 (برترین/پیشنهاد/استوری) · phase7=34 (نظر/پرسش/گزارش) · phase8=29 (نوبت/پیام/هشدار) · phase9=36 (صاحبان/تصاحب/تغییرات/نشان) · phase10=32 (پلن/قابلیت/اشتراک/تبلیغ) · phase11=18 (محتوا/ظاهر/فلگ) · phase12=26 (رسانه/مشتری/دنبال/اعلان) · phase13=23 (audit/سپر/آمار) · phase14=30 (سطل/تنظیمات/پشتیبان/نمونه) · phase15=12 (تلفیق).
- **جمع رگرسیون نهایی: ۳۹۴ سبز.**
- نکته‌های اجرا: phase13 شبیه‌سازی اسکنر با XFF می‌کند (شناسهٔ پویا؛ ضربه‌ها ۶۰ث در حافظه) · phase14 دادهٔ نمونه می‌سازد/پاک می‌کند (بنر بذر را خودش بازمی‌گرداند) · phase15 نام دکان 214 را برمی‌گرداند · urllib بی‌UA به‌عنوان bot ثبت می‌شود (برای تست بازدید واقعی UA مرورگری بفرست) · بعد از phase12 صحنهٔ 555/556 را بازچینش کن (الگو در خود فایل).

---

## و) ممیزی جامع پنل — نقشهٔ نهایی

- ۱۶ فاز (۰ تا ۱۵) همه ✅ — شناخت + مدرک سبز + باگ/رفع + کاندیدهای ردِ هر فاز در `work/dastgir/QA-panel-audit-plan.md`.
- **۲۴ باگ محصول رفع‌شده (#17–#40):** بحرانی‌ها: #18 (XSS ذخیره‌شدهٔ پنل)، #30 (خاموشی همهٔ کلیدها در هر ذخیره)، #39 (WAL/قفل — کارایی)، #24 (بی‌اعتبارسنجی کامل نوبت). خانواده‌های بزرگ: #26 (۹مسیر)، #27 (۱۱مسیر)، #33/#35/#36 (جعل موفقیت). مهم‌های داده‌ای: #17 #20 #21 #23 #29 #34 #40.
- **ارتقاها:** A1 (یادآوری استقرار — رمز پنل!) · A2 ✅v167 · A3 ✅v168 · A4 ✅v170 · A5 ✅v170. ارتقای دِدوپ صف تغییرات ✅v175.
- باگ‌های ابزارخودی (دفترچه برای درس): #19 #22 #31 #37 #38 #41 #42.
- ماتریس دسترسی مدیر (مدرک فاز ۰) هم در دفترچه است.

---

## ز) سرور، استقرار، انتشار

- **VPS ثابت:** `root@130.185.78.237` · `/opt/dastgir` · سرویس `bazarche` + nginx · آپدیت: زیپ از x0.at → `bash /opt/dastgir/deploy/update.sh /root/FILE.zip` · **دست‌نخورده روی سرور:** `data/bazarche.db`، `uploads/`، `chat-files/`.
- **x0.at:** `curl -sS -A "curl/8.5" -F "file=@F" https://x0.at` (بدون احراز؛ v164 سابقاً /879009d4).
- **تعطیل به فرمان کاربر («فعلاً بیخیال»):** دامنه/DNS/HTTPS/ورود پنل زنده. NSهای پارس‌پک قبلاً جمع‌بندی شده.
- **یادآوری A1 هنگام استقرار:** روی سرور زنده «رمز عبور پنل» حتماً تنظیم شود — تا رمز نیست، لو رفتن کلید = ادمین کامل. (همچنین: چرخش توکن، site_url واقعی، کلید پیامک برای OTP واقعی، زرین‌پال/پوش نیازمند HTTPS.)

## ح) کارهای باز

1. **آپلود v175 به x0.at** — انجام نشده (تحویل لوکال بود).
2. **استقرار زنده/دامنه** — تعطیل تا فرمان کاربر.
3. آپلود زیپ‌های جدید به ریپوی گیت‌هاب = کار خود کاربر (پوش از سندباکس نمی‌شود).
4. هر کار تازه از فرمان کاربر شروع می‌شود — با قانون ۳ (وضعیت ریپو اول اعلام شود) و کار از نسخهٔ تازه‌آپلودیِ ریپو.

## ط) چک‌لیست شروع چت جدید

1. این فایل (و در صورت نیاز عمیق‌تر، بخش‌های فنی پیوست همان فایل) را بخوان.
2. وضعیت ریپو را اعلام کن: آخرین شناخته `main @ 239b2bf` (= v152 کاربر)؛ کارِ محلی = **v175** در `work/dastgir` (مدل تحویل: هر دور کار با نسخهٔ آپلودی تازهٔ کاربر ادامه پیدا می‌کند).
3. اگر کاربر zip تازه در ریپو گذاشت: آن منبع حقیقت است — باز کن، باتری‌ها را اجرا کن، ادامه بده.
4. RULES.md را هم بخوان (قوانین همین فایل + هر قاعدهٔ تازه‌ای که در چت‌های بعدی اضافه شود).
5. سرور تست را روشن کن، پیش‌شرط‌های بخش هـ را چک کن، بعد کار را شروع کن.
6. هیچ ادعایی بدون مدرک. دروغ ممنوع. دقت بر سرعت مقدم.

---
---

# 📎 پیوست — دانش‌نامهٔ کامل نسل دوم (دورهای ۱–۱۹، تا v117)
> متن زیر عیناً فایل `FULL-HANDOFF (1).md` ریپو است و برای مرجع فنی عمیق حفظ شده.
> ⚠️ دو تصحیح نسبت به متن قدیمی: (۱) مسیر ورک‌اسپیس الان `/home/user/work/dastgir/` است نه `/home/user/repo-369/site/`؛ (۲) نسخه/نشانگرها از v117 به v175 رسیده‌اند (بخش الف بالا). بقیهٔ دانش فنی (معماری/DB/امنیت/تست/UX/درس‌ها) همچنان معتبر است.

# 🧭 دانش کامل پروژه «بازارچهٔ ایران» — فایل ادامهٔ کار در صفحهٔ تازه

> **این فایل چیست؟** تمام دانشی که در کل همکاری (ممیزی نسل دوم، دورهای ۱ تا ۱۹) کسب شد،
> self-contained در همین یک فایل. در صفحهٔ جدید فقط همین فایل را بده تا کار از صفر شروع نشود.
> هر چیزی که اینجا «مدرک» دارد یعنی واقعاً اجرا/تست شده، نه ادعا.
> تاریخ نگارش: ۲۰۲۶-۰۸-۳۱ (پس از اتمام دور ۱۹). اعداد فنیِ انتشار (نشانگر کش و نام زیپ)
> صرفاً واقعیت‌های عملیاتی لازم برای ادامه‌اند، نه موضوع بحث.

---

## فهرست
1. شناسنامهٔ پروژه و محیط
2. خط‌قرمزهای کاربر (هرگز نشکن)
3. قوانین رفتاری کاربر (RULES)
4. سبک ارتباطی کاربر
5. راه‌اندازی و دستورهای روزمره (کپی‌پیست‌شونده)
6. تله‌های محیط سندباکس (مهم‌ترین بخش برای شروع بدون سردرگمی)
7. معماری کد (مسیر درخواست + ماژول‌ها + قوانین)
8. دیتابیس (جدول‌ها، کوکی‌ها، purge، ایندکس‌ها، تله‌ها)
9. کش و کارایی (اعداد و تصمیم‌ها)
10. امنیت (لایه‌ها + چیزهای راستی‌آزمایی‌شده)
11. تست‌ها (۳۹ سوئیت + ابزارها + قواعد اجرا)
12. دفتر یافته‌ها F-001 … F-035 (همه)
13. خلاصهٔ دورهای ۱ تا ۱۹ (چه شد، چه درس)
14. تصمیم‌ها و سلیقه‌های ثبت‌شدهٔ محصول
15. استانداردهای UX/موبایل که نگهبان دارند
16. نقشهٔ کد (فایل:نماد ≈ خط) — برای پیدا کردن سریع
17. روش انتشار/تحویل (زیپ + بوت‌تست + بامپ نشانگر)
18. وضعیت فعلی ورک‌اسپیس و سرور
19. کارهای باز و گام‌های بعدی محتمل
20. فهرست اسناد مرجع در ورک‌اسپیس
21. دانش خارجی جذب‌شده از تصاویر کاربر (سرعت تصاویر/سئو، Agent Teams، Cloudflare)

---

## ۱) شناسنامهٔ پروژه و محیط

- **نام:** «بازارچهٔ ایران» (قبلاً «بازارچهٔ نجف‌آباد») — دایرکتوری/مارکت‌پلیس کسب‌وکارهای
  ایران با الگوی **دیوار** (انتخاب شهر/استان، چت درون‌برنامه‌ای، علاقه‌مندی، مرتب‌سازی، عکس‌دار).
- **پشته:** پایتون ۳ خالص (سرور `http.server` سفارشی `ThreadingHTTPServer`) + SQLite (WAL) +
  HTML/CSS/JS دست‌نویس. **صفر وابستگی خارجی** — خط قرمز: باید روی **Pydroid 3 اندروید** اجرا شود.
  (Playwright فقط ابزار تستِ سندباکس است، نیازمندی سایت نیست.)
- **پورت:** 8080 (عمداً نه 8000 — تداخل کوکی/SW با نمونهٔ قدیمی). run.py اگر پورت گرفته بود
  از `find_free_port` یکی دیگر برمی‌دارد (مثلاً 8081).
- **نقش‌ها:** مدیر کل `/panel` · دکان‌دار `/login` (OTP یا رمز) · مشتری `/me/login` (اختیاری) ·
  بازدیدکنندهٔ بی‌حساب.
- **ریپوی گیت‌هاب (منبع حقیقت):** `https://github.com/rezadb369-wq/369.git` — حساب `rezadb369-wq`،
  برنچ main. (آدرس قدیمی کاربر `g9742888-droid` غلط بود؛ ریپوهای 963/film وجود ندارند.)
  آخرین بررسی: ریپو فقط زیپ حاوی نسخهٔ پیشین را داشت؛ کارِ تازه هنوز **با خودِ کاربر باید
  آپلود شود** (پوش گیت از سندباکس بدون اعتبار ممکن نیست).
- **مسیر ورک‌اسپیس:** `/home/user/repo-369/site/` (ریشهٔ بسته؛ `python3 run.py` از همین‌جا).
  پوشهٔ `data/` (دیتابیس) محلی است و **هرگز** داخل زیپ/ریپو نمی‌رود.
- **زبان همکاری:** فارسی محاوره‌ای؛ کد/نام متغیر/کامیت انگلیسی. منطقهٔ زمانی: Asia/Tehran.
- کاربر روی گوشی اندروید کار می‌کند؛ توضیح‌ها ساده و صادقانه، بدون اصطلاحات سنگین.
- کاربر **توسعه‌دهندهٔ رسمی نیست** — سایت را با یک عامل هوش مصنوعی می‌سازد و تحویل می‌گیرد؛
  پس توضیح‌ها باید ساده/صادقانه باشند و انتظار فنی از او نداشته باشیم.
- سندباکس: پایتون 3.13، ۲-هسته، CPU/IO محدود (در تفسیر بنچ‌مارک‌ها لحاظ شود).

## ۲) خط‌قرمزهای کاربر (عیناً از HANDOFF — همگی معتبر)

1. **«دروغ نگو»** — هرگز نگو «درست شد» مگر واقعاً تست شده باشد: curl با کد وضعیت، مرورگر برای
   خطای کنسول، چک دیتابیس. اگر نشد، صادقانه بگو.
2. **موبایل‌اول از همه‌چیز مهم‌تر** — هر تغییر در ۳۶۰/۳۹۰/۴۱۲px (و ۳۲۰ برای حالت‌های مرزی):
   بدون سرریز افقی، هدف لمسی ≥۴۴px، فونت بدن ۱۴px، هیچ متن غیرضروری زیر ۱۲px.
3. **صفحهٔ اول = مهم‌ترین بخش دیده‌شدن.** هیرو و ظاهر اول همیشه اولویت اول.
4. **«لوپ ۲۰ بار»** — بعد از تمام‌شدن هر کار، دوباره از اول دنبال اشکال واقعی بگرد؛ زود جمعش نکن.
5. **«اجازه نپرس»** — کار را بکن، منتظر تأیید نمان (فقط در ابهام واقعاً مهم بپرس).
   ⚖️ استثنا: پیام‌های چنددستوری → اول برنامه (قانون ۲).
6. **هر تحویل = یک زیپ تازه با نام جدید** — بدون `data/`، `raw/`، `shots/`، `__pycache__`، لاگ.
7. **معماری مهم است** — لایه‌های تمیز، بدون خدا-فایل، مستند.
8. **امنیت اولویت همیشگی** — «تست نفوذ قدرتمند» همیشه در برنامه باشد.

## ۳) قوانین رفتاری (RULES.md — خلاصهٔ کامل)

- **قانون ۱: «حرف من را به پرامپت قوی‌تر تبدیل کن»** — هر پیام کاربر = پرامپت خام. قبل از اجرا
  بازنویسی‌شده‌اش را کوتاه نشان بده، با این قالب دقیق:
```
🎯 پرامپت دقیق‌شده
هدف:            <یک جمله — چه چیزی باید درست شود>
دامنه:          <کدام فایل/صفحه/بخش>
معیار پذیرش:    <چه چیزی که دیدنش = تمام شدن کار>
خط‌قرمزها:      <چه چیزهایی نباید بشکند>
روش تأیید:      <curl / تست / مرورگر / چک دیتابیس>
خروجی تحویلی:   <زیپ تازه + گزارش + …>
```
  ابهامِ واقعاً مهم → فقط همان یک مورد را بپرس؛ وگرنه بلافاصله اجرا کن.
- **قانون ۲: پیام بلند = تقسیم کار** — اول برنامه بفرست (تعداد بخش = تعداد دستورها، بدون
  ادغام)، هر پیام فقط یک بخش تا «تأییدشده»، بعدِ هر بخش تیک ⏳→✅ و نمایش برنامهٔ به‌روز:
```
📋 برنامهٔ کار — N بخش
1) <عنوان> — فایل‌ها: … | معیار پذیرش: …
2) …
ترتیب پیشنهادی: ۱ ← ۲ ← ۳
این پیام فقط روی بخش X کار می‌کند.
```
  **استثنا:** اگر صریحاً گفت «همه‌اش را یک‌جا انجام بده»، تقسیم نکن.
  تعارض با «اجازه نپرس»: قانون ۲ مقدم است — برنامه اول می‌آید، ولی اگر جوابی نیامد از
  بخش ۱ شروع کن و منتظر نمان.
- **قانون ۳:** منبع حقیقت ریپوی گیت‌هاب. شروع هر چت: `git ls-remote --heads https://github.com/rezadb369-wq/369.git`
  + مقایسهٔ آخرین زیپ ریپو با ورک‌اسپیس + گزارش یک‌خطی («ریپو: … | ورک‌اسپیس: … | وضعیت: …»).
- **قانون ۴:** هر قاعده/سلیقهٔ تازهٔ کاربر را همان لحظه در RULES.md (قاعدهٔ رفتاری) یا
  HANDOFF.md (سلیقه/تصمیم پروژه) ثبت کن و به کاربر بگو «ثبت شد». قانون ثبت‌نشده = فراموش‌شده.
- **قانون ۵: ابزارها/اسکیل‌های معرفی‌شدهٔ کاربر (هر ۵ مورد اصالتشان تأیید شد):**
  - **Stop Slop** (hardikpandya/stop-slop، ~۱۶هزار ستاره) → سبک نوشتنِ جذب‌شده: بدون عبارت
    پرکننده و قید زائد؛ صدای فعال با فاعل انسانی؛ عینی و مشخص (نه کلی‌گویی)؛ ریتم جمله‌ها
    متنوع؛ بدون em-dash؛ اعتماد به شعور مخاطب؛ جملهٔ شعاری ننویس. **روی پاسخ فارسی هم اعمال می‌شود.**
  - **UI/UX ProMAX** (nextlevelbuilder، ~۱۲۱هزار ستاره) → هوش طراحی (پالت، کامپوننت، ریتم
    بصری) مبنای کارهای طراحی بازارچه.
  - این دو، اسکیلِ Claude Code اند و در محیط Arena به‌صورت slash-command نصب نمی‌شوند؛
    پس فقط **محتوای مفیدشان جذب شد** (در سبک نوشتن و طراحی همین پروژه اعمال می‌شود).
  - روی PC خود کاربر (در Claude Code) قابل نصب‌اند: **Skill Creator** (anthropics/skills) ·
    **Remotion** (ساخت ویدیو/موشن با پرامپت) · **FireCrawl** (اسکرپ وب؛ نیازمند کلید رایگان
    firecrawl.dev).
  - در این سندباکس اسکیل اجرا نمی‌شود؛ اگر کاربر ویدیوی واقعی خواست، Remotion جدا راه
    می‌افتد (روی محیط PC خودش).
- **قانون ۶: بازسازی به‌جای وصله** — اگر راه‌حلی بعد از تست درست از آب درنیامد، آن بخش کلاً
  حذف و با دانش و دقت بیشتر از نو ساخته می‌شود، حتی اگر ساعت‌ها طول بکشد؛ برای انیمیشن/UX
  از حساب‌های گیت‌هاب بزرگ تخصصی و اسکیل‌های معرفی‌شده (Stop Slop، UI/UX ProMAX، FireCrawl
  برای تحقیق) استفاده شود؛ تحقیق + تست تا درست‌شدن ادامه می‌یابد و دست از کار برداشته نمی‌شود.
- **قانون ۷: تم رسمیِ استاندارد** — هویت سایت «رسمی» است: خاکستری خنثی + یک رنگ برند (آبی
  اداری). هویت لوکس طلایی/لاجوردیِ قدیمی کامل حذف شد؛ هاله، گرادیان رنگی، بافت و بلور شیشه‌ای
  ممنوع. theme-night/light.css فقط «توکن» نگه می‌دارند؛ override ساختاری یا هگز مستقیم در
  آن‌ها = متن محو در یکی از دو حالت = خطای تست (tools/test_appearance.py همین را می‌گیرد).
  رنگ جدید فقط توکن معنایی در tokens.css با کنتراست AA در «هر دو» حالت؛ contrast_audit باید
  ۰ خطا بدهد. کف متن ۱۲px در هر دو حالت؛ تعویض تم با محو شدن ۳۰۰ms؛ بازدید اول از تمایل
  سیستم؛ ترجیح قدیمی «luxury» هم پذیرفته می‌شود (سازگاری بازگشت)

## ۴) سبک ارتباطی کاربر

- پیام‌ها کوتاه، مستقیم، فارسی محاوره‌ای (گاهی بدون نقطه‌گذاری).
- نارضایتی = تکرار موضوع با جمله‌های کوتاه → اول مشکل را دقیق قبول کن، ریشه را پیدا کن، درست کن، با مدرک نشان بده.
- راست‌گویی را تحسین می‌کند حتی با خبر بد. اعداد و نتیجهٔ تست می‌خواهد ببیند (چند آزمون/چند موفق).
- از شتاب‌زدگی بدش می‌آید: **«دقت بر سرعت مقدم است»** (شعار کل ممیزی).
- با اسکرین‌شات از گوشی خودش گزارش می‌دهد؛ در این محیط می‌توان تصویر پیوست را دید (read_file)
  **و** ابعاد عناصر را با Playwright اندازه گرفت.
- «قبل از اجرا تحقیق کن» — برای تغییر مهم، اول منابع (MDN، web.dev، استانداردها) بعد پیاده‌سازی.

## ۵) راه‌اندازی و دستورهای روزمره

```bash
# سرور (اگر مرده بود — بین نوبت‌ها می‌میرد)
cd /home/user/repo-369/site && python3 run.py        # یا با start_process

# سلامت سریع
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8080/

# کلید پنل این نصب
python3 -c "import sys;sys.path.insert(0,'server');import db;print(db.owner_token())"
# پنل: GET /panel?key=<token>   (با رمزِ تعیین‌شده: ورود دومرحله‌ای با کوکی bzv2_admin)

# دادهٔ دمو (۱۹ کسب‌وکار، ۱۷۴ دسته) — «همیشه» بعد از باتری کامل اجرا کن
python3 tools/seed_demo.py

# اجرای یک تست / باتری کامل
BZ=http://127.0.0.1:8080 python3 tools/test_X.py
export BZ=http://127.0.0.1:8080
pass=0; fail=0; for t in tools/test_*.py; do python3 "$t" >/tmp/out.txt 2>&1 \
  && pass=$((pass+1)) || { fail=$((fail+1)); echo "FAIL $t"; tail -6 /tmp/out.txt; }; done; echo "PASS=$pass FAIL=$fail"

# ممیزی‌های مرورگری (نیاز به chromium دارد — بخش ۶)
BZ=http://127.0.0.1:8080 python3 tools/mobile_audit.py      # پنل: ۲۴ صفحه × ۴ دستگاه
BZ=http://127.0.0.1:8080 python3 tools/public_audit.py      # عمومی: ۲۵ صفحه × ۴ دستگاه
BZ=http://127.0.0.1:8080 python3 tools/contrast_audit.py   # ۱۲ صفحه × ۲ تم
python3 tools/bench_load.py        # بار کلی
python3 tools/bench_panel.py       # ۱۸ صفحهٔ پنل، سرور ایزوله (BZ_DATA_DIR)
python3 tools/bench_1000.py        # ۱۰۰۰ کاربر واقع‌گرایانه + رمبش

# Playwright خام (سنجهٔ DOM/اسکرین‌شات) — الگوی تستیِ آماده در تاریخ دور ۱۹ کار شده
```

**متغیرهای محیط:** `BZ` (آدرس سرور برای تست‌ها، پیش‌فرض :8080) · `BZ_MAX_WORKERS=64` (سقف نخ) ·
`BZ_SHIELD_RATE/WINDOW` (سقف سپر؛ لوپ‌بک مستثنی مگر XFF بفرستد) · `BZ_DATA_DIR` (دادهٔ ایزوله برای بنچ).

## ۶) تله‌های محیط سندباکس — قبل از هر چیز بخوان

1. **سرور بین نوبت‌ها کشته می‌شود** → اول curl بزن؛ اگر مرده بود با `start_process` (نه bash
   بلندمدت) بالا بیاور. نام فرایندِ_user-friendly بده (مثلاً «Site v117 ux map pricing»).
2. **تلهٔ pkill:** الگوی «run.py» در cmdline خودِ bashِ فراخوان هم می‌آید و خودش را می‌کشد —
   همیشه از `stop_process`/`fuser -k <port>/tcp` استفاده کن، نه pkill با الگوی عام.
3. **باینری Playwright در `~/.cache/ms-playwright/` است و `.cache` در snapshot نمی‌ماند** (حتی وسط
   گفتگو می‌پرد!). بازیابی: `python3 -m playwright install chromium` (پکیج pip معمولاً می‌ماند).
   **نصب اولیه حتماً با `--with-deps`**: `python3 -m playwright install --with-deps chromium`
   (بدون آن: خطای libnspr4.so). بدون مرورگر: ۸+ سوئیت exit=1 می‌شوند و ۲ آزمونِ
   security/robustness **بی‌صدا skip** می‌شوند (عدد کمتر از سند دیده می‌شود — فریب نخور).
4. **خروجی تست را با pipe/grep قضاوت نکن** — کرش وسط اسکریپت بی‌صدا می‌ماند؛ همیشه exit code بگیر.
5. **read_file روی PNG بینایی ندارد** (در این محیط) — اسکرین‌شات فقط بایگانی؛ داوری UX با
   **سنجهٔ DOM در Playwright** (getBoundingClientRect/computed style).
6. ترتیب لود CSS: tokens → main → pages → **mobile-refine** → mobile-standard → theme-*.
   اگر override در pages.css بنویسی و mobile-refine.css قانون متناقض داشته باشد، **مورد اخیر
   می‌بَرد** — override را در آخرین فایلِ مؤثر بگذار (تجربهٔ دور ۱۹ با bill-toggle).
7. فایل‌های خارج از `/home/user` ماندگار نیستند؛ زیپ/بوت‌تست موقت را در `/tmp` بزن و بعدش پاک کن
   (و سرور موقتش را با `fuser -k` ببند — با curl مطمئن شو 000 شد).
8. snapshot ورک‌اسپیس `node_modules`, `.venv`, `__pycache__`, `dist` و… را ذخیره نمی‌کند؛
   سقف ~۱۲۸MB/۱۰هزار فایل.
9. **بینایی تصویر در سشن ممکن است نباشد** (read_file روی PNG/JPG بینایی نمی‌دهد) →
   جایگزینِ آزموده و کارا: OCR فارسی — `sudo -n apt-get install -y tesseract-ocr
   tesseract-ocr-fas` سپس `tesseract img.jpg out -l fas+eng` (نکتهٔ عجیب: خروجی با
   پسوند دوتایی `.txt.txt` ساخته می‌شود). دقت فارسی ~۹۰٪: اعداد و کلمات لاتین جابه‌جا
   می‌شوند — اعداد مهم را با زمینه بازسازی کن و در ثبت دانش علامت «بازسازی‌شده» بزن.

## ۷) معماری کد

### مسیر یک درخواست
```
مرورگر → Handler (server/app.py)
  1) shield.check()          نرخ/اسکنر/UA → 429/404/403 (لوپ‌بک مستثنی؛ XFF فقط از لوپ‌بک)
  2) GC ساعتی (فقط do_GET)   تصاویر چت ۷روزه + trash_purge + retention_purge
  3) route_get()/do_POST     ~۱۸۱ شاخهٔ مسیر
  4) auth                    کلید پنل (مقایسهٔ constant-time) + رمز PBKDF2 + admin_sessions
                            سشن مالک/مشتری = کوکی HMAC · OTP با سهمیه per-phone
  5) db.rate_ok              سهمیهٔ معنایی هر کنش (سهمیه‌های امنیتی با always=True مستقل از کلید اسپم)
  6) pagecache (مهمان خالص)  TTL ۸s/۱۲۸ کلید، کلید = اثرانگشت محتوا (بخش ۹)
  7) views_*.py              HTML سرور-رندر با ui.py (کروم + توکن تم)
finally: ui.clear_context() + db.request_done()  (آزادسازی قفل راحت‌کار thread)
```
- خطاها: هر استثنا → صفحهٔ ۵۰۰ بدون جزئیات + traceback فقط در کنسول. `do_HEAD` = `do_GET`.

### ماژول‌های سرور (server/ — ۲۰ فایل)
| فایل | ~خط | نقش |
|---|---|---|
| db.py | 5596 | ۲۷۸+ تابع؛ SCHEMA + `_reconcile_schema()` (ستون غایب→ALTER خودکار)؛ PBKDF2 600k؛ otp؛ sessionها؛ rate_ok؛ purgeها؛ page_fingerprint؛ sparkline_svg |
| app.py | 3602+ | Handler، مسیریابی، auth، آپلود، robots/sitemap، client_key، _inline_edit (allow-list)، کش/ابطال |
| views_panel.py / views_panel2.py | 1412/2197 | پنل مالک (دو بخش) |
| views_owner.py | 1746 | پنل دکان‌دار |
| views_public.py / views_public2.py | 1571/962 | صفحات عمومی (map_page در views_public.py ~966) |
| views_catalog.py | 626 | کاتالوگ/مقایسه/کشف |
| ui.py | 1212 | کروم/منوی پایین/هدر تم/GA4/بامپ نشانگر کش `?vNNN` (۱۰ نقطه) |
| features.py | 1164 | چت/سفارش/تیکت/علاقه‌مندی/چانه (price_items_from_catalog) |
| uploads.py | 548 | ذخیره فایل، magic-bytes، EXIF-strip، فشرده‌سازی stdlib |
| views_customer.py / views_chat.py / views_extra.py | 508/219/153 | پنل مشتری / چت / ریز |
| analytics.py | 325 | visits واقعی (IP/UA/dev/cid/sid/ref؛ بات‌ها bot=1) — هرگز raise نمی‌کند |
| pagecache.py | کوچک | کش TTL‌دار صفحات مهمان |
| shield.py | ~150 | سپر نرخ/اسکنر/UA + _sweep دوره‌ای |
| geoip.py | ~200 | IP→استان/شهر فارسی، bisect روی CSV ۱۲٬۶۶۱ رنج، کش سقف‌دار ۸۱۹۲، پوشش ۹۷.۱٪؛ IPv6/گپ → خالیِ صادقانه (استان می‌ماند) |
| qr.py | 274 | QR (SVG فقط هندسه) |
| sms.py | 109 | اطلاع‌رسانی مالک (الگوی کاوه‌نگار؛ غیرفعال مگر تنظیم؛ هرگز raise نمی‌کند) |
| run.py | 201 | اجرا + find_free_port |

### نقشهٔ مسیرها (~۱۸۱ شاخه در app.py)
- **عمومی:** `/` · `/businesses` · `/b/<slug>` · `/catalog` · `/map` · `/blog` · `/events` ·
  `/compare` · `/nearby` · `/lists` · `/deals` · `/favorites` · `/prices` (خاموش=۴۰۴) ·
  `/contact` · `/submit` · `/cities` · `/areas` · `/categories` · `/cart` · `/order/ok`
- **API:** `/api/stories` · `/api/nearby` · `/api/businesses` · `/api/search` · `/api/favorite` ·
  `/api/follow` · `/api/review-helpful` · `/api/question-helpful` · `/api/check-duplicate` ·
  `/api/alert` · `/alert` · `/prices/alert` — جست‌وجوی عمومی = `/businesses?q=` (مسیر `/search`
  عمومی **وجود ندارد**؛ live-search مال کاتالوگ است — اشتباه ابزار‌سازِ دور ۱۶ همین بود).
- **مشتری `/me/*`:** login(+verify/password) · profile · chats · orders · favorites · account ·
  notifications · following · name · logout · **export** (CSV «داده‌های من»)
- **دکان‌دار `/my/*`:** dashboard · businesses · chats · orders · stories · trash · reviews ·
  bookings · inquiries · claim · questions · offers · tickets · plan · account · export ·
  story/add · logout · admin-chat
- **پنل `/panel/*` (~۴۵ صفحه):** businesses(+bulk) · categories · reviews(+bulk) · reports(+bulk) ·
  tickets(+bulk) · trash · traffic · analytics(+export) · chats · owner-chats · orders · ads ·
  badges · plans(+payment) · subscriptions · events · edits · alerts · insights · inbox ·
  features · unlocks · lists · deals · stories · editor · audit(+export CSV) · customers ·
  broadcast · follows · areas · demo · backup(+export/full/import) · media(+gc/delete) ·
  settings · appearance · security · rotate-token · login/logout · inline-edit · **search** ·
  **moderation** (صف تعدیل ۷بخشی)
- **استاتیک:** `/assets/*` · `/sw.js` · `/robots.txt` · `/sitemap.xml` · `/offline.html` ·
  `/manifest.webmanifest` · `/favicon.ico` · `/qr` · `/vcard` · `/chat-file/*` · `/ticket-file/*`
- **دروازه‌ها (رفتار مرز):** `/my`,`/me` بدون سشن → 303 به ورود · `/panel` بدون کلید → 403 ·
  `/chat-file`,`/ticket-file` بدون نقش → 404 · متدهای غیرمعمول (PUT/DELETE/…) → 501 تمیز.

**قوانین معماری:** نماها هرگز SQL مستقیم نمی‌زنند (فقط db.py) · import غیراستاندارد ممنوع
(نگهبان test_open) · JS فقط رفتار، نه داده · فایل test_foo.py هر رفعِ foo را قفل می‌کند.

### فرانت‌اند (site/assets)
- **JS (۲۰ فایل):** app.js (اصلی، DOM-XSS-safe با createElement/textContent) · mappage.js (نقشهٔ شهر:
  setSelected کارت پایینی می‌سازد ~:60؛ listener دکمهٔ «نزدیک من» ~:407) · map.js (بک‌اند لیفلت،
  zoomControl خاموش + `L.control.zoom({position:'bottomleft'})`، addMarker/bindPopup ~:150-260) ·
  chat/cart/compare/favorites/stories/upload/compress/bulk/qa/social/catpicker/nearby/countdown/…
- **CSS (۸ فایل، به‌ترتیب لود):** tokens.css (توکن معنایی؛ --tap:44px، --tap-lg:48px) ·
  main.css (~۲۰۰۰ خط؛ کف‌های لمسی بخش ۱؛ `.btn-icon` 44×44) · pages.css (~۳۶۰۰؛ .map-me،
  .map-selected، .has-card، .price-grid/.price-card ~:755) · mobile-refine.css (overrideهای موبایل —
  **بعد از pages لود می‌شود**) · mobile-standard.css · theme-night/light.css (فقط توکن).
- **نقشه:** leaflet vendored در `assets/vendor/leaflet/` (CDN فقط fallback است — عمدی).
- **PWA:** `sw.js` با `VERSION='bazarche-v117'`؛ network-first برای HTML، cache-first برای assets.

## ۸) دیتابیس

- مسیر: `data/bazarche.db` (+ -wal/-shm). WAL؛ اتصال per-thread کش می‌شود (`_PersistentCon`).
  (نکتهٔ عملیاتی: فایل -wal حدود ۴MB روی آستانهٔ auto-checkpoint می‌ماند — **عادی است**،
  خطا نیست؛ بازیابی پس از kill سخت آزموده و سالم است.)
- **فهرست ۵۷ جدول (برای پرس‌وجوهای سریع):** admin_sessions, ads, alerts, audit, badges,
  biz_badges, biz_features, bookings, businesses, categories, chat_attachments, chat_messages,
  chat_threads, cities, cityevents, claims, coupons, customers, customer_sessions, edits,
  events, favorites, flashdeals, follows, haggles, inquiries, items, messages, notifications,
  order_items, orders, otp, owners, pages, plans, points, posts, price_alerts, price_history,
  questions, ratelimit, redemptions, reports, review_scores, reviews, searches, sessions,
  settings, stories, story_views, subscriptions, ticket_attachments, ticket_msgs, tickets,
  toplists, trash, visits — به‌علاوهٔ ۶ آبجکت FTS.
  (نکتهٔ تاریخی: امتیاز/کوپن/تبادل از محصول حذف شده‌اند؛ جدول‌هایشان می‌ماند ولی مسیر زنده ندارند.)
- **۵۷ جدول معمولی + ۶ آبجکت FTS + ۴۱ ایندکس + ۰ تریگر** (عدد قطعی؛ پشتیبان export پنل ۵۱ جدولِ
  داده + کلید _meta دارد — عمداً بدون sessions/otp/ratelimit/fts/cities).
- **تلهٔ تست:** برای DB آزمایشی، `db.DB_PATH` را **قبل از هر فراخوانی db** در فرایند تازه عوض کن
  (یا `BZ_DATA_DIR`)، وگرنه init به DB واقعی می‌خورد. تراکنش باز اسکریپت تست → قفل SQLite →
  `database is locked` سرور → بعد از هر INSERT تستی فوراً commit کن.
- **کوکی‌ها:** `bzv2_sess` (دکان‌دار) · `bzv2_cust` (مشتری) · `bzv2_admin` (پنل) — همه HttpOnly +
  SameSite=Lax + Secure-if-HTTPS. کوکی قدیمی `bz_sess` در هر مسیر (حتی کش‌هیت) پاک می‌شود
  (`clear_legacy_cookies`).
- **سشن‌ها:** ادمین دو-پله: سشنِ فقط-کلیدی = `locked`؛ POST/رمز فقط با `unlocked`
  (`admin_session_new(locked=)` / `admin_session_ok(tok, unlocked_only=)`)؛ TTL ۷روز؛
  rotate کلید همهٔ سشن‌ها را می‌کشد. OTP: per-phone ۵/۱۵دقیقه + per-IP ۲۰/۱۵دقیقه؛
  کد فقط روی لوپ‌بک نمایش داده می‌شود.
- **ایندکس‌های ساخته‌شده در init()** (idempotent، روی DB موجود اعمال می‌شود):
  orders(biz_id) · orders(customer_id) · order_items(order_id) · notifications(customer_id,read)
  ترکیبی/covering · follows(customer_id,biz_id) · follows(biz_id) · stories(biz_id).
- **Purgeها:** در init + جاروی ساعتی: IP ناکاری visits>90d · ratelimit>7d ·
  visits/audit/searches>2y · تصاویر چت ۷روزه · trash (item/review/report=24h، story=7d).
- ADMIN_SESSION_TTL، PBKDF2_ITERS=600_000 (rehash-on-login از هش‌های قدیمی ۱۲۰k).

## ۹) کش و کارایی

- **pagecache.py:** کش در-حافظهٔ صفحات مهمانِ عمومی (مسیرهای ثابت + `/b/<slug>` و `/item/<id>`
  با regex)؛ TTL ۸ ثانیه، سقف ۱۲۸ کلید؛ آمار بازدید/کوکی در `send()` ثبت می‌شود (کش آمار نمی‌بلعد).
- **کلید کش = اثرانگشت محتوا** (`db.page_fingerprint()` ~:1410): md5 از settings+businesses کامل +
  شمارش‌های سبک items/reviews/plans/… (ستون‌های پرمصرف مثل views بیرون). نوشتن از **هر فرایندی**
  (پنل/تست/CLI) کلید را تازه می‌کند + ابطال فوری با هر POST و ست‌کننده‌های محتوا.
- **قاعدهٔ طلایی (F-031): کش فقط برای «مهمانِ خالص»** — اگر هر کوکی احرازی
  (bzv2_sess/bzv2_cust/bzv2_admin) در درخواست هست، cacheable=False و رندر تازه
  (چون قلب علاقه‌مندی `data-fav`/`aria-pressed` سمت سرور برای مشتری واردشده رندر می‌شود).
  منطق در app.py در route_get **بعد از** محاسبهٔ cacheable.
- **میکروکش بین‌پرداختی (get_settings) عمداً حذف شد** — تست با HTTP می‌نوشت و کشِ فرایند خودش
  کهنه می‌ماند. همان دام را دوباره نساز.
- `request_queue_size` (backlog) = 512 — رمبش ۱۰۰۰ اتصال دیگر ریست نمی‌دهد، صف می‌شود.
- **اعداد اثبات‌شده (سندباکس ۲-هسته):** ۶۴ هم‌زمان `/`: p50 1.34s→**18ms** (~۷۰×) ·
  ۱۰۰۰ کاربر واقع‌گرایانه: **۵۰۰۰/۵۰۰۰، صفر ۵xx/قفل/ریست** · رمبش ۳×۱۰۰۰: p50≈0.9s max≈1.9s
  (~۴۷۰rps) · سقف پایدار صفحات کش‌نشده ≈۲۴۰rps (GIL) · تأخیر تک‌کاربر ۶-۱۱ms · RSS فلات ~۲۰۰MB ·
  مسیرهای داغ DB بعد از ایندکس‌ها ۰.۰۰۴-۰.۳۲ms (قبلاً تا ۸.۷ms؛ بهبود ۲۷× تا ۵۱۰×).
- **اندازهٔ پاسخ‌های کلیدی (مرجع مقایسه، بدون کوکی):** `/` 200 (~138KB) · `/catalog` 200 (~39KB) ·
  `/map` 200 (~51KB) · `/login` 200 (~39KB) · `/panel` بدون کلید 403 (~17KB) · `/prices` خاموش 404.
- قبول صادقانه: «۲۰۰ برای موجودیت ناموجود» جعلی نبود — 303→/login بود که urllib دنبال کرده بود.

## ۱۰) امنیت (چک‌لیست راستی‌آزمایی‌شده — همه با مدرک)

- **shield:** ۱۲۰req/10s→429 · ۳۰ throttle پیاپی→مسدود ۱۰دقیقه · مسیر اسکنر ۳ ضربه/60s→مسدود
  ۳۰دقیقه · UA ابزار مخرب→403 · `_sweep()` هر ≤۵دقیقه حافظه را جارو می‌کند.
- آپلود عمومی: magic-bytes + سقف ۳MB + نام تصادفی + EXIF-strip + ضدtraversal ✓
  (سقف کل بدنهٔ درخواست ۳۰MB؛ `save_media` استوری سقف حجم جدا ندارد — طراحی‌شده و کامنت دارد)
- پیوست چت: structural check + chmod600 + regex نام سخت ✓ (فقط عکس — میراث قدیمی، بی‌خطر)
- سرو فایل چت/تیکت: **اول نقش، بعد خواندن دیسک** + private,no-store ✓
- هدرها: CSP/XFO/nosniff/Referrer/Permissions + HSTS پشت پروکسی ✓ · CRLF غیرممکن (quote کامل) ✓
- کلید پنل: مقایسهٔ constant-time؛ فقط یک‌بار در URL (ورود با key سشن می‌سازد؛ ۱۰۷+ ریدایرکت و
  ۵۸+۱۶ لینک و فیلدهای مخفی بدون key؛ noindex دوطرفه). نمایش «لینک ورود پنل» در تنظیمات عمدی است.
- رمزها: PBKDF2 600k + rehash-on-login؛ رمز پنل حداقل ۶ نویسه؛ سقف ۱۰ تلاش/۱۵دقیقه.
- قیمت سفارش همیشه از DB بازخوانی می‌شود (`features.price_items_from_catalog`) — قیمت کلاینت
  فقط برای ردیف آزاد (بدون item_id) پذیرفته است.
- `featured` فقط با perk پلن (`db.can(bid,"featured")`) نوشته می‌شود؛ بدون perk کلید ارسال نمی‌شود
  (مقدار ادمین حفظ می‌شود).
- کلیدهای سرویس (sms/tg/zarinpal) در فرم ماسک؛ ارسال خالی = حفظ؛ gmaps عمومی-به‌طراحی (فقط ماسک UI).
- OTP از کلید اسپم جدا (`rate_ok(..., always=True)` برای otp:/otpip:/pw:/apl:).
- `_safe_next()` واحد برای ریدایرکت بعد از ورود (شروع /، نه //، بدون .. و \، منع /panel، <۳۰۰).
- XSS: escape سراسری در همهٔ ویوها؛ کاناری زندهٔ ۶ صفحه فقط اسکیپ‌شده؛ پنل صفر XSS؛
  DOM سمت کلاینت با textContent.
- بن/بلاک دکان‌دار و مشتری: مرگ فوری سشن + رد ورود «این حساب مسدود شده است.» + آن‌بن.
- robots.txt: مسیرهای خصوصی + بلاک خزنده‌های AI.
- **جاروی‌های اثبات:** ۶۶۰ GET مرزی + ۶۵ POST آشغال + ۱۲ متد عجیب = صفر ۵xx/Traceback؛
  ۱۱۹ صفحهٔ پنل + ۱۲۰ کوئری خصمانه = دو باگ ۵۰۰ که رفع شد (F-019)؛ pentest 38/0؛ abuse 15/0.

## ۱۱) تست‌ها و ابزارها (tools/ — ۳۹ سوئیت test_* + ابزارها)

**وضعیت پایه: باتری ۳۹/۳۹ سبز (exit=0 همه) · test_all 152/152.**

سوئیت‌های کلیدی و اعدادشان:
- هسته: test_all **152** · test_v3 153 · test_open_parity 525 ترکیب · test_gates 83 · test_open
- امنیت: test_security **11**/0 (با مرورگر نصب!) · test_pentest 38 · test_abuse 15 · test_panel_security **43**
- استحکام: test_robustness 7 · test_panel_robust 16 · test_migration 17 · test_isolation 31
- پنل/UX: test_panel_ux 32 · test_panel_mod 28 · test_appearance 39
- زیرسیستم‌ها: actions 55 · admin_extras 43 · analytics 56 · chat_v73 99 · onboarding 40 ·
  favorites_v74 27 · context 28 · section1 27 · section4_v75 34 · remaining_v76 27 · prices 53 ·
  haggle 61 · tickets 42 · trash 38 · privacy 19 · privacy_v117 14 · geoip_fa 17 · audit_fixes 34+
- مرورگر: test_browser (a11y@1440 + واکنش‌گرایی ۱۱ عرض) · mobile_audit (۲۴ صفحهٔ پنل×۴ دستگاه=۹۶) ·
  public_audit (۲۵ صفحهٔ عمومی×۴=۱۰۰) · contrast_audit (۱۲ صفحه×۲تم، باید صفر خطا)
- ابزار غیرتستی: seed_demo · bench_load · bench_panel · bench_1000 · fuzz_routes (جاروی مسیر) ·
  shot_sweep · walkthrough_e2e · layout (fa()) · data/cities/taxonomy/build_sprite/…

**قواعد تست:**
1. تست‌ها `BASE` را از env `BZ` می‌خوانند و به سرورِ بالا نیاز دارند.
2. **بعد از هر باتری کامل: `python3 tools/seed_demo.py`** (test_all دیتای دمو را پاک می‌کند —
   شبیه‌سازی نصب تازه؛ عمدی).
3. تست‌ها باید hermetic باشند (به دیتای ازقبل-موجود حساس نباشند) — نمونه‌های اصلاح‌شده:
   چک مالک با دلتا (+۱) نه شمارش مطلق؛ edit_approval=0 در شروع؛ founder-score خنثی‌سازی؛
   فازر تنظیمات self-restoring (snapshot+restore).
4. flaky معروف: رنج IP 80.191/16 چنداستانی است — هرگز شهری را هاردکد نکن؛ با `geoip.lookup(ip)` مقایسه کن.
5. هر رفع = قفل در سوئیت (الگوی `test_audit_fixes.py` §F-XXX). نگهبان نسخه: `test_migration`
   تست «نسخهٔ sw» را دارد که **عمداً هر انتشار باید همگام بامپ شود**.
6. تست analytics روی سرورِ آرام (فقط تست‌ها) — وگرنه اعداد شلوغی می‌گیرد.

## ۱۲) دفتر یافته‌ها (همه — رفع‌شده مگر خلافش گفته شود)

| شناسه | شدت | شرح یک‌خطی | وضعیت |
|---|---|---|---|
| F-001 | ⚪ | کار پنل ادعاییِ سشن قبلی در ریپو نبود → بازسازی کامل در فاز ۵ | ✅ بازسازی شد |
| F-002 | 🔵 | متغیر مردهٔ EXEMPT_LOOPBACK در shield | ✅ |
| F-003 | 🔵 | رشد بی‌کران دیکشنری‌های حافظهٔ shield → _sweep دوره‌ای | ✅ |
| F-004 | ⚪ | عدد جدول‌های اسناد (۶۴) غلط → ۵۷+۶FTS در اسناد اصلاح شد | ✅ |
| F-005 | ⚪ | اختلاف شمارش تست‌ها = skip بی‌صدای مرورگرِ غایب (درس) | ✅ |
| F-006 | ⚪ | فرایند سرور بین turnها کشته می‌شود (محیط) | ثبت |
| F-007 | 🟡 | تست flaky analytics (تهرانِ هاردکد روی رنج چنداستانی) → انتظار = شهر واقعی geoip | ✅ |
| F-008 | ⚪ | نبود Playwright = ۸ سوئیت ترکید + ۲ skip بی‌صدا | ✅ (راه‌حل ثبت) |
| F-009 | 🟡 | سهمیهٔ OTP به کلید اسپم گره خورده بود → rate_ok(always=True) | ✅ |
| F-010 | 🔵 | PBKDF2 ۱۲۰k → ۶۰۰k + rehash-on-login | ✅ |
| F-011 | 🔵 | بهداشت: docstring rate_ok؛ مقایسهٔ OTP با != (ریسک پذیرفته) | ✅ |
| F-012 | 🟡 | قیمت سفارش به نقل کلاینت → بازخوانی از کاتالوگ همان دکان | ✅ |
| F-013 | 🔵 | اکوی کلیدهای سرویس در فرم تنظیمات → ماسک + حفظ مقدار خالی | ✅ |
| F-014 | 🔵 | دو نسخهٔ ناسازگار _safe_next → تابع واحد | ✅ |
| F-015 | 🟠 | ۷ جدول داغ بدون ایندکس → ۷ ایندکس covering (۲۷× تا ۵۱۰×) | ✅ |
| F-016 | 🟡 | جعل featured بدون perk + پاک‌شدن مقدار ادمین هنگام ویرایش مالک → گیت پلن | ✅ |
| F-017 | 🔵 | fa(None)→«None» و sparkline TypeError نظری → دفاع یک‌خطی | ✅ |
| F-018 | 🔴 | ورود با رمز پنل مرده بود (فرم key را POST نمی‌کرد) | ✅ |
| F-019 | 🟡 | دو باگ ۵۰۰: page=abc و unlock شناسهٔ ناموجود (FK) | ✅ |
| F-020 | — | لاگ ممیزی بدون فیلتر/CSV → فیلتر action/detail/بازه + CSV با BOM | ✅ |
| F-021 | — | نبود صف تعدیل → /panel/moderation هفت‌بخشی با عملیات سریع | ✅ |
| F-022 | — | نام لاتین شهرها در گزارش فارسی → _fold یونیکد + ~۲۰۰ شهر؛ پوشش ۸۵→۹۷.۱٪ | ✅ |
| F-023 | — | purgeها فقط در بوت → جاروی ساعتی (retention_purge) | ✅ |
| F-024 | — | خروجی دادهٔ مشتری نداشت → GET /me/export (CSV با BOM، فقط ردیف‌های customer_id) | ✅ |
| F-025 | ⚪ | test_all دمو را پاک می‌کند → قرارداد seed_demo بعد از باتری + هشدار زرد در خروجی | ثبت |
| F-026 | — | کنتراست حلقهٔ دکمهٔ استوری (conic) می‌شکست → سطح صاف + حاشیهٔ رنگ‌میکس | ✅ |
| F-027 | — | نشان‌های ؟/✓ ۳.۲:۱ در شب → جفت توکن on-color (۷:۱/۷.۹:۱) | ✅ |
| F-028 | — | badge اعتماد با رنگ خام DB ۳.۴:۱ → متن foreground + tint رنگ | ✅ |
| F-029 | — | شمارنده‌های ۱۱px زیر کف ۱۲px → ۱۲px | ✅ |
| F-030 | 🟠 | سریالی‌شدن رندر زیر رمبش → pagecache (TTL8/128) + کلید اثرانگشت + backlog 512 | ✅ |
| F-031 | 🟡 | شخصی‌سازی در صفحات کش‌شده → کش فقط مهمان خالص | ✅ |
| F-032 | — | قیمت پلن‌ها ۱۲px (قاعدهٔ `.nums` سراسری) → حذف از فهرست؛ 27.2px | ✅ |
| F-033 | — | کلیک پین دو پنل باز می‌کرد → حذف popup لیفلت در نقشهٔ شهر؛ فقط کارت غنی | ✅ |
| F-034 | — | دکمهٔ «نزدیک من» با کارت باز ۲۳۰px کش می‌آمد → بالا-چپ + حذف قانون bottom | ✅ |
| F-035 | 🔵 | کف لمس ۴۰px در .todo-item .btn-icon (صف تعدیل) → ۴۴px | ✅ |

**صفر یافتهٔ باز.**

## ۱۳) خلاصهٔ کارهای انجام‌شده (دور ۱→۱۹)

- **دور ۱-۲:** کلون ریپو، استخراج بسته، باتری پایه ۳۲/۳۲، راستی‌آزمایی رفع‌های ممیزی قبلی،
  کشف F-001 (کار پنل گم‌شده) و flaky تست analytics.
- **فاز ۱ امنیت (دور ۳-۶):** خواندن کامل لایهٔ امنیتی + دور اصلاح: F-002/009/011/012/013/014؛
  سوئیت قفل test_audit_fixes؛ زیرساخت امنیت «واقعاً قوی» ارزیابی شد.
- **فاز ۲ سلامت (دور ۷):** integrity/FK صفر تخلف؛ round-trip پشتیبان ۵۱ جدول؛ فروپاشی+بازیابی WAL؛
  عدد جدول‌ها قطعی؛ کشف F-015.
- **فاز ۳ باگ‌ها (دور ۸):** fuzz_routes ۶۶۰ درخواست مرزی صفر 5xx؛ رفع F-016.
- **فاز ۴ کارایی (دور ۹):** ۷ ایندکس (تا ۵۱۰×)؛ جاروی shield؛ بنچ ۵۰۰ کلاینت صفر خطا.
- **فاز ۵ پنل (دور ۱۰-۱۴) شش مرحله:** م۱ امنیت (نشت کلید از ~۱۰۰ نقطه، سشن ۷روزه دو-پله،
  PBKDF2 600k، رفع F-018، 43/0) · م۲ استحکام (16/0، فازر self-restoring) · م۳ صفحه‌بندی ۱۳ تابع/
  ۱۴ ویو + حذف N+1 (orders ۱۷۳۰KB→۱۰۸KB؛ 18/18 زیر ۵۰۰ms) · م۴ موبایل/UX (bulk سه لیست،
  /panel/search، pillهای COUNT، فولد فیلترها، 32/0) · م۵ تعدیل (/panel/moderation هفت‌بخشی،
  فیلتر+CSV ممیزی، بن E2E، 28/0) · م۶ اسناد+انتشار.
- **فاز ۶ داده/حریم خصوصی (دور ۱۵):** GeoIP فارسی ۹۷.۱٪؛ retention ساعتی؛ /me/export؛ قفل‌ها 17+14.
- **فاز ۷ UX عمومی (دور ۱۶):** public_audit ۱۰۰ اجرا صفر؛ کنتراست ۱۲×۲ صفر؛ ۴ رفع کنتراست (F-026-029).
- **فاز ۸ جمع‌بندی (دور ۱۷):** بستن F-017/F-004؛ همگام‌سازی اسناد؛ زیپ؛ بوت‌تست.
- **دور ۱۸ — تست بار ۱۰۰۰ کاربر:** F-030 کش+backlog، F-031 کش فقط-مهمان، bench_1000؛
  ۵۰۰۰/۵۰۰۰؛ باتری ۳۹/۳۹ و test_all hermetic 152/152.
- **«رفع و فایل نهایی»:** تحویل زیپ کامل.
- **دور ۱۹ — UX موبایل تعرفه‌ها+نقشه:** F-032..F-035 + الگوی کنترل شبح + کارک نقشه فشرده‌تر +
  toggle تعرفه در یک ردیف تا ۳۲۰px؛ mobile_audit 96/96؛ باتری ۳۹/۳۹؛ زیپ ۳۵۰ فایل rebuild شد.

## ۱۴) تصمیم‌ها و سلیقه‌های ثبت‌شدهٔ محصول

- الگوی دیوار (شهر/استان، چت، علاقه‌مندی، مرتب‌سازی، عکس‌دار). ویرایش فقط از پنل (نه سایت عمومی).
- تنها کانال پشتیبانی عمومی = **تیکت** (تماس/واتساپ پشتیبانی حذف شد).
- قیمت کالاها در نصب تازه **خاموش** (`prices_enabled=0`)؛ `/prices` در حالت خاموش = 404.
- آیکون‌ها: کیفیت > حجم؛ کوانتیزاسیون/سیاه‌کردن شفافیت ممنوع؛ آیکون تأییدشده را «بهینه» نکن.
- تم رسمی استاندارد (قانون ۷)؛ رنگ جدید فقط توکن معنایی با AA هر دو تم.
- ویجت شناور → مهمان را به ورود، کاربر را به تیکت‌هایش می‌برد.
- ویجت recently-viewed مقاوم‌سازی‌شده؛ گیرندهٔ طرح‌ها/استوری از پنل دکان‌دار.
- کاربر می‌خواهد ویدیو/عکس را خودش با ابزار AI بسازد (پرامپت از ایجنت، تولید از کاربر).
- «نمایش لحظه‌ای وضعیت دکمهٔ نزدیک‌من» در span مخفی sr-only می‌ماند چون JS وضعیت را در آن می‌نویسد.
- نام زیپ هنوز `bazarche-najafabad-*.zip` است در حالی که سایت «بازارچهٔ ایران» شده — تصمیم تغییر نام با کاربر (باز).

### تصمیم‌های تاریخیِ حذف/تغییر که هنوز لازم‌الرعایه‌اند (بی تاریخِ نسخه)
- **تغییر نام سراسری «مغازه» → «دکان»** در کل UI — واژهٔ «مغازه» را در متن‌های تازه برنگردانید.
- **تفکیک پنل‌ها:** «پنل من» (شخصی) از «پنل دکان» (کسب‌وکار) جدا شد؛ سایدبار تکراری دکان حذف
  شد؛ جایگاه چهارم نوار پایین موبایل = پنل دکان.
- **سیستم وفاداری (امتیاز/کوپن/تبادل) کلاً حذف شد** — اگر کاربر خواست، از نو و آگاهانه
  طراحی شود (جدول‌های قدیمی هنوز در اسکیما هستند، مسیر زنده ندارند).
- **چت سبک تلگرام؛ پیوست چت فقط عکس** (بدون pdf/ویدئو در چت)؛ پرسش ناشناس `/ask` حذف شد —
  ارتباط مهمان فقط از فرم‌های عمومی/تیکت.
- **نوار پایین موبایل شیشه‌ای با آیکون شناور** — نسخهٔ فعلی استاندارد و بازطراحی‌های قبلی مرده‌اند.
- **استوری‌ها:** ویدئو حداکثر ۳۰ ثانیه + تریم + فشرده‌سازی سمت کلاینت قبل از آپلود +
  پیش‌نمایش خود دکان‌دار + نوار پیشرفت real-time + بازدید ناشناس‌سازی‌شده.
- **آمار بازدید واقعی** (نه شمارش ساده): IP/UA/دستگاه/کوکی + بات=1 · شهر بازدیدها و ساعت
  اوج در پنل · UTM + خروجی CSV · GA4 اختیاری (id با regex `^G-[A-Z0-9]{6,12}$`).
- **سطل بازیافت نرم‌حذف** با retention و بازگردانی از پنل و `/my/trash`.
- **فشرده‌سازی رسانه روی سرور** (stdlib، بدون re-encode سنگین) + «همیشه-فشرده» قبل از آپلود.
- **صفحهٔ ترافیک/سپر در پنل:** کاشی‌های ۴۲۹/اسکنر/بات/مسدودی + «برداشتن مسدودی» (تلهٔ CGNAT).

## ۱۵) استانداردهای UX/موبایل (نگهبان‌دار)

- **هدف لمسی ≥۴۴px** (کف --tap) — نگهبان: mobile_audit/public_audit. الگوی مجاز «کنترل شبح»:
  لمس کامل ۴۴×۴۴ + هستهٔ بصری ~۳۰px شفاف (`color-mix(card 60%, transparent)` + `backdrop-filter: blur(8px)`).
- بدون سرریز افقی در ۳۲۰/۳۴۴/۳۶۰/۳۹۰/۴۱۲/768. فونت بدن ۱۴px؛ حداقل ۱۲px هر متن.
- RTL کامل؛ `inset-inline-*` به‌جای چپ/راست (به‌جز موارد فیزیکی عمدی مثل map-me بالا-چپ).
- تعرفه‌ها در ≤768px: قیمت clamp(1.35rem,8vw,1.7rem)؛ توضیح ۲ خط clamp؛ toggle صورتحساب در
  ≤360px بدون wrap (چهار دکمهٔ ۴۴px در یک ردیف).
- نقشهٔ شهر: «نزدیک من» بالا-چپ فقط-آیکون شبح؛ زوم پایین-چپ شبح؛ **کلیک پین فقط کارت پایینی**
  (popup فقط در نقشهٔ تک‌مغازهٔ صفحهٔ کسب‌وکار)؛ کارت سقف ۴۰٪ ارتفاع نقشه.
- a11y: کنتراست AA هر دو تم (contrast_audit صفر)؛ آیکون‌فقط = aria-label؛ متای viewport/alt/برچسب.

## ۱۶) نقشهٔ کد (فایل:نماد — خط‌ها تقریبی و رانده می‌شوند؛ با grep نماد را پیدا کن)

- **کش:** `server/pagecache.py` (کل کد) · `db.page_fingerprint()` db.py ~1410 · منطق cacheable/
  guest-check در `app.py` route_get بعد از محاسبهٔ cacheable · `clear_legacy_cookies` در هیت.
- **نقشه:** `views_public.py map_page` ~966 (payload پین‌ها + map-me با aria-label) ·
  `mappage.js setSelected` ~60 (کارت ms-head/meta/row/descr/acts + کلاس has-card) ·
  listener دکمهٔ me ~407 · `map.js` leafletBackend/addMarker/bindPopup ~150-260 ·
  CSS: `.map-me` و بلوک کنترل شبح pages.css (جایگاه قدیمی ~711) · `.map-selected` ~2106 ·
  `.map-stage.has-card` ~2155 (قانون جابه‌جایی legend فقط) · تعرفه‌ها `.price-grid/.price-card` ~755 ·
  overrideهای دور ۱۹ در انتهای pages.css + `mobile-refine.css` (bill-toggle ~:50).
- **پنل:** `views_panel.py` (بخش ۱ + ماسک کلیدها ~1105) · `views_panel2.py` (moderation/audit/traffic) ·
  helper صفحه‌بندی `_page_result/_page_slice` در db.py و `panel_pager` در ویوها · bulk در app.py
  `/panel/{reviews,reports,tickets}/bulk` · `/panel/search` در app.py + `panel_search` در ویو.
- **سایر:** `_safe_next` و `client_key` و `_inline_edit` و `is_spambot/safe_int` در app.py ·
  `rate_ok(always=)` و purgeها و `retention_purge` در db.py · `_sweep` در shield.py ·
  `_fold/_CITY_KEYS` در geoip.py · `fa()` در tools/layout.py · بامپ `?vNNN` در ui.py (۱۰ نقطه) ·
  `VERSION` در site/sw.js · نگهبان نسخه در tools/test_migration.py.

## ۱۷) روش انتشار/تحویل

1. **ریبیلد زیپ** (از ریشهٔ بسته؛ `-D` یعنی بدون مدخل پوشه‌ها — الگوی بستهٔ اصلی همین است):
```bash
cd /home/user/repo-369/site
rm -f ../bazarche-najafabad-v117.zip
zip -rqDX ../bazarche-najafabad-v117.zip . \
  -x "shots/*" -x "site/shots/*" -x "data/*" -x "raw/*" -x "*__pycache__*" -x "*.pyc" \
  -x "*.db" -x "*.db-shm" -x "*.db-wal" -x "chat-files/*" -x "*.log" -x "*.zip"
# انتظار: ~۳۵۰ فایل، ~۴.۳MB — اگر بیشتر شد، فهرست را با unzip -l بگرد (مدخل پوشه/شات/دیتا نشته باشد)
unzip -t ../bazarche-najafabad-v117.zip | tail -1   # ← «No errors detected»
```
2. **بوت‌تست از دل زیپ** (در /tmp استخراج → run.py → curl چند مسیر: `/` و `/pricing` و `/map`
   و `/contact` = 200؛ `/panel` بدون کلید = 403؛ صفر Traceback در لاگ) → بعد `fuser -k <port>/tcp`
   و اطمینان از بسته‌شدن (curl باید 000 بدهد) و پاک‌کردن /tmp.
3. **بامپ نشانگر اگر CSS/JS/asset عوض شد:** `?vNNN` در ui.py **و** `VERSION` در sw.js **و** تستِ
   نگهبان نسخه در test_migration.py — هر سه با هم.
4. **اسناد:** WHATS-NEW.md (بالای فایل، بخش تازه) · README/ARCHITECTURE اعداد ·
   audit/03 (یافته‌ها) · audit/05 (جدول تغییرات) · audit/00 (گزارش وضعیت روز).
5. **بعد از باتری: seed_demo.** تحویل با present_file زیپ. آپلود به ریپو = کار خودِ کاربر
   (پوش از سندباکس ممکن نیست).

## ۱۸) وضعیت فعلی ورک‌اسپیس و سرور (لحظهٔ نگارش)

- بستهٔ کار: `/home/user/repo-369/site/` — **باتری ۳۹/۳۹ سبز · test_all 152/152 ·
  mobile_audit 96/96 · دادهٔ دمو سالم (seed اجرا شد)**.
- زیپ تحویلی: `/home/user/repo-369/bazarche-najafabad-v117.zip` — ۳۵۰ فایل، ~۴.۳MB،
  تست یکپارچگی ✓ بوت‌تست ✓ شامل همهٔ تغییرات تا پایان دور ۱۹. (زیپ قدیمی v116 هم کنارش هست.)
- سرور پیش‌نمایش زنده: فرایند «Site v117 ux map pricing» روی پورت 8080 (اگر مرده بود: بخش ۵).
- اسکرین‌شات‌های before/after دور ۱۹: `site/shots/{before,after}-{pricing,map,map-click}-390.png`
  (بایگانی — داوری با سنجهٔ DOM).
- اسناد ممیزی: `/home/user/audit/` (00 برنامه، 01 دانش‌نامه، 02 فهرست فایل، 03 یافته‌ها،
  04 خط پایه، 05 تغییرات، 06 بازسازی پنل).
- حافظه‌های اصلی ریپو: `CLAUDE (2).md` (تاریخچهٔ کامل نسخه‌های قدیمی + درس‌ها)،
  `HANDOFF (4).md` (شناسنامهٔ همکاری تا پیش از ممیزی نسل دوم)، `RULES (1).md`.

## ۱۹) کارهای باز و گام‌های بعدی محتمل

- **آپلود زیپ فعلی به ریپوی گیت‌هاب توسط کاربر** (تنها قدم ماندهٔ انتشار).
- تصمیم باز: تغییر نام زیپ از `najafabad` به چیز سراسری‌تر — با کاربر.
- تصمیم باز: تحویل به‌صورت درخت فایل در گیت به‌جای زیپ تکی (diff قابل‌دیدن) — با کاربر.
- کارهای همیشگیِ یادآوری برای کاربر: چرخاندن توکن پنل؛ روی دامنهٔ واقعی `site_url` + کلید پیامک
  (بدون SMS، کد OTP فقط لوکال نمایش داده می‌شود)؛ زرین‌پال/پوش‌نوتیفیکیشن نیازمند هاست+HTTPS.
- اگر درخواست UX جدید آمد: الگوی کار دور ۱۹ را تکرار کن — سنجهٔ before با Playwright (getBoundingClientRect/
  computed style) → تشخیص ریشه → رفوع با قفل نگهبان ≥44px → بعد/سنجه → mobile_audit + باتری → seed →
  اسناد (03/05/00/WHATS-NEW) → ریبیلد زیپ + بوت‌تست.
- موارد موبایل معلقِ تاریخی (از ممیزی‌های قدیمی، عمدتاً رفع‌شده ولی اگر کاربر گریزد دوباره بگرد):
  tilt کارت‌ها در ۳۲۰px، پنل «همین حالا باز هستند» در موبایل.

## ۲۰) درس‌های کلیدی که دوباره نباید تکرار شوند (فشرده)

- anchor پچ را با خط بعدی تأیید کن (assert count==1) · `%`-formatting در SQL ممنوع (پارامتری) ·
  endpoint تازه را curl کن · خروجی pipe گمراه است، exit code بگیر · batch غیرهمگام visits ممنوع ·
  backlog≥512 نگه دار · `hours` نه hours_map · میکروکش بین‌پرداختی نساز · کش همیشه به fingerprint گره ·
  seed_demo بعد از باتری · test_analytics روی سرور آرام · pkill تله است · playwright با --with-deps ·
  `.cache` می‌پرد · سرور بین نوبت‌ها می‌میرد · override CSS را در آخرین فایل لودشده بگذار ·
  هر رفع قفلِ تست دارد · هیچ تغییری بدون ثبت در audit/05 نماند.
- **توکن سطح ≠ توکن متن:** `--color-muted` رنگِ سطح است و `--color-muted-foreground` رنگ متن —
  قاطی‌کردن‌شان = متن هم‌رنگ پس‌زمینه در یک تم (تلهٔ معروف متن‌های آپلود استوری).
- قواعد ساختاری ممکن است در فایل‌های تمِ «خالی‌شده» پنهان بمانند — بعد از هر تمیزکاری تم،
  test_appearance + contrast_audit را در **هر دو تم** اجرا کن.
- ممیزی a11y مرورگری در 1440px اجرا می‌شود نه موبایل — موبایل را mobile_audit/public_audit بگیر.
- ویدئو/اسپرایت سنگین هیرو حذف و هیرو CSS-محور شده (سرعت موبایل) — برنگردانید مگر درخواست صریح.
- `node --check` روی هر JSِ تغییریافته، قبل از تست‌ها.

---

## ۲۱) دانش خارجی جذب‌شده (تصاویر تحویل‌شده توسط کاربر — ۲۰۲۶-۰۸-۳۱ شب)

> منبع: ۱۰ تصویر در `/home/user/uploads/` — ۸ استوری اینستاگرام از @ramin_amirhaeri
> (سری آموزش «بهبود سرعت لود سایت با تصاویر در سئو») + ۲ اسکرین‌شات اپ Sunista.
> متن با OCR استخراج شد (بولد ۹ بخش ۶)؛ اعدادِ خراب‌خوانده با زمینه بازسازی و
> علامت‌گذاری شده‌اند؛ انتهای دو اسکرین‌شات ناقص خوانده شد (ذکر شده).

### الف) چک‌لیست سرعت تصاویر (سری @ramin_amirhaeri)
- گوگل تجربهٔ کاربر را با **Core Web Vitals** می‌سنجد؛ مهم‌ترین: **LCP** (Largest
  Contentful Paint) — هدف خوب **زیر ۲.۵ ثانیه** [عدد بازسازی‌شده از OCR]. مقصر همیشگی:
  تصویر بزرگ/هیروِ بالای صفحه.
- الگوی بدِ بار اول: بنر ~۱.۸MB + thumbnail ~۶۰۰KB + چند PNG چندمگابایتی [اعداد
  بازسازی‌شده] — کاربر باید همه را با همین اینترنت دانلود کند.
- راه‌حل‌ها: تبدیل **WebP** (مرجع: developers.google.com/speed/webp — تأیید گوگل؛ روی
  ۱۰۰+ سایت تست‌شده، حتی بدون هاست دانلود/CDN) · ابعاد درست · Compression ·
  **Responsive Images (srcset)** برای تصاویر پایین صفحه · **Lazy Load فقط برای پایین
  صفحه — هرگز روی LCP/Hero** · Cache/CDN.
- نسخهٔ وردپرسی (افزونهٔ Converter for Media: بکاپ → فعال‌سازی → خروجی WebP → حفظ
  اصل‌ها → Bulk Optimization؛ و AVIF برای نتیجهٔ واقعی) — **برای بازارچه نامربوط**
  (وردپرس نیست)؛ فقط به‌عنوان الگوی گام‌ها ثبت شد.

**وضعیت بازارچه در همین محورها (چک اجرایی همان شب):** تصاویر دسته‌ها ۱۸۵ فایل همگی
WebP ✓ · lazy-load در ۱۴ نقطهٔ ویوها ✓ · هیرو CSS-محور بدون تصویر سنگین (LCP-safe) ✓ ·
کش ایستا `max-age=31536000, immutable` + ETag ✓ · فشرده‌سازی کلاینت+سرور و EXIF-strip ✓.
**تنها شکاف واقعی:** انکود WebP از آپلودی‌های کاربران با کتابخانهٔ استاندارد پایتون
نمی‌شود (قانون صفر-وابستگی/Pydroid) → بهبود پیشنهادی آینده: `srcset`/ابعاد صریح برای
تصاویر آپلودی؛ تبدیل خودکار WebP روزی که قید صفر-وابستگی برداشته شود.

### ب) Agent Teams در برابر subagent (اسکرین‌شات از @mahbodpour)
- یک Agent برای کارهای متمرکز، مرحله‌به-مرحله یا تغییرِ چند بخشِ وابسته = انتخاب
  کم‌هزینه‌تر.
- subagentها برای بررسی‌های مستقل و کوتاه مناسب‌اند؛ هرکدام کارش را انجام می‌دهد و
  نتیجه را فقط به Agent اصلی برمی‌گرداند.
- Agent Teams وقتی ارزش دارد که اعضا [ادامهٔ متن در تصویر ناقص خوانده شد — اگر روزی
  لازم شد از کاربر پرسیده شود].
- پیوند با این پروژه: این فایل دانش مستقل از مدل/ایجنت نوشته شده؛ در محیطی با
  چند-ایجنت، دادن همین فایل به ایجنت اول کافی است.

### پ) حملهٔ بات‌ها و Cloudflare (اسکرین‌شات دوم)
- نشانه‌های حمله در سرور/سایت → راهکار: قوانین اختصاصی Cloudflare یا **Bot Fight Mode**.
- ⚠️ هشدار مهم: Bot Fight Mode در پلن رایگان کلادفلر **حتی ربات گوگل را هم می‌گیرد**
  → به‌جایش تنظیم دقیق‌تر بررسی شود [انتهای جمله در تصویر ناقص بود].
- پیوند با بازارچه: معادلِ روی-سرورِ همین دفاع الان shield.py است (نرخ/اسکنر/UA/مسدودی
  + برداشتن مسدودی برای CGNAT)؛ هنگام مهاجرت به دامنهٔ واقعی پشت Cloudflare، این
  هشدار دربارهٔ ایندکس‌شدن در گوگل جدی گرفته شود.

*پایان فایل.*

**یادداشت خودکفایی (مهم):** این فایل برای ادامهٔ کار **کامل و خودکفا** است — همهٔ دانش
لازم برای کار بعدی (قوانین، محیط، معماری، تست‌ها، یافته‌ها، وضعیت، روش انتشار) داخل همین
فایل است. ارجاع‌های زیر فقط «آرشیو عمیق‌تر» برای سشن فعلی‌اند (در ورک‌اسپیس فعلی موجودند؛
در صفحهٔ تازه در دسترس نخواهند بود مگر خودتان آپلودشان کنید):
`audit/03-FINDINGS-LOG.md` (متن کامل مدرکِ هر یافته: فایل:خط + جداول راستی‌آزمایی هر فاز) ·
`audit/01-KNOWLEDGE.md` (دانش‌نامهٔ کد + دفتر میزان مطالعهٔ فایل‌ها) ·
`audit/00/02/04/05/06` (برنامه/فهرست فایل/خط پایه/جدول تغییرات/بازسازی پنل) ·
`CLAUDE (2).md` (روایت کامل نسخه‌های قدیمی حذف‌شده از این فایل: v72–v116) ·
`HANDOFF (4).md` + `RULES (1).md` (ریشهٔ بخش‌های ۲-۴ این فایل).
