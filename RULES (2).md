# 📋 قوانین کاری این همکاری — ثبت‌شده در ۲۰۲۶-۰۹-۰۵

## دامنهٔ مسئولیت من
- **فقط نسخهٔ ۱۵۲ به بعد وظیفهٔ من است.** نسخه‌های قدیمی‌تر (تا v151) فقط زمینه/مرجع‌اند.
- کار همیشه از نسخهٔ تازه‌آپلودشدهٔ کاربر در ریپو شروع می‌شود (منبع حقیقت = ریپو گیت‌هاب).

## گردش کار تحویل (قانون تحویل زنجیره‌ای)
1. **متن طولانی/چنددستوری → اول برنامه.** قبل از هر اجرا، برنامهٔ مرحله‌به‌مرحله می‌سازم
   (تعداد بخش = تعداد دستور، بدون ادغام)؛ هر پیام فقط یک بخش انجام می‌شود؛ بعد از هر بخش
   تیک ⏳→✅ و نمایش برنامهٔ به‌روز.
2. **هر تحویل = فایل کامل سایت (زیپ کامل)** — حتی اگر فقط یک تغییر کوچک داخلش باشد.
3. **زنجیرهٔ پاک: بعد از تحویل فایل جدید، فایل تحویلی قبلی از ورک‌اسپیس پاک می‌شود**
   تا فضای کاری همیشه روان بماند (در هر لحظه حداکثر یک فایل تحویلی روی صفحه است).
4. تغییر جزئی در فایل‌های سرور/asset → بامپ نشانگر `?vNNN` در ui.py + `VERSION` در sw.js
   + نگهبان نسخه (هر سه با هم) — طبق روش انتشار handoff.

## خط‌قرمزهای کاربر (عیناً — هیچ‌کدام نباید رخ دهد)
- ❌ **دروغ** — هیچ ادعای «درست شد» بدون مدرک تست واقعی (curl + کد وضعیت، exit code، چک دیتابیس، مرورگر).
- ❌ **تنبلی** — راه میان‌برد زدن، نیمه‌کاره گذاشتن، به‌جای ریشه‌رفع وصله زدن.
- ❌ **سریع کار کردن** — دقت بر سرعت مقدم است؛ شتاب‌زدگی ممنوع.
- ❌ **اشتباه / بی‌دقتی** — هر تغییر دوبار بازبینی می‌شود؛ «لوپ ۲۰ بار»: بعد از تمام‌شدن کار
   دوباره از اول دنبال اشکال واقعی می‌گردم.
- ❌ **خروجی بدون اطمینان و بدون تحقیق گسترده** — قبل از تغییر مهم، اول تحقیق (منابع، کد موجود،
   تست قبل/بعد)؛ خروجی فقط وقتی داده می‌شود که راستی‌آزمایی کامل شده باشد.

## وضعیت شروع (۲۰۲۶-۰۹-۰۵)
- ریپو: `main @ 239b2bf` — `dastgir-v152.zip` (آپلود امروز ۱۱:۱۰ تهران، جایگزین v141).
- ورک‌اسپیس: کلون تازه همگام با ریپو ✓
- v152 = «بخش ۷ فاز ۱: شش اصلاح بازبینی کاربر» — نشانگرها تأیید شد: sw.js `bazarche-v152` ✓،
  ui.py `?v152` ✓، شواهد ثبت‌شدهٔ سشن سازنده: 15-PASS 0-FAIL.

## ورودی کار ۶+۷ (v156) — قراردادها و قواعد تازهٔ همین کار
- **بخش ۶ — نقشه:** پین کوچک‌تر + کوچک‌ترشدن *واقعی* با زوم در هر ۴ حالت:
  نقطهٔ انتخاب‌نشده (`.bz-dot` 14px در قاب لمس ۳۰×۳۰)، پین فعال (`.bz-pin-drop`
  26px محتوا + قاب ۲px)، پین دکان (همان نقطه، رنگ ویژهٔ ویژه‌ها)، «موقعیت من»
  (`.bz-me-dot` 13px). فرمول مقیاس هر دو موتور: `min(1.1, max(0.42, 0.87^(z−14)))`
  — با زوم *بیشتر*، پین *کوچک‌تر* (رفع رفتار معکوس قدیمی). مقیاس روی عنصر
  *داخلِ* divIcon می‌خورد نه روی ریشه (ترنسفورم جابه‌جایی Leaflet خراب نمی‌شود).
- **بخش ۷ — فراخوان دکان:** دسترسی با هر یک از سه مسیر هم‌ارز: پرک `broadcast`
  در پلن اشتراک، بنیان‌گذارانِ اولین-N (settings.founder_limit)، گرهٔ دستی مدیر
  (biz_features) + نشان «بنیان‌گذار». «موجود شد خبرم کن» از دو جا: صفحهٔ کالای
  ناموجود و درون گفتگو. رساندن **فقط** زنگولهٔ داخل سایت (kind="broadcast"،
  push=False) با احترام به تنظیمات هر مشتری (کلید ۸م: «فراخوان‌ها»، پیش‌فرض روشن).
- درخواست بازِ تکراری رد می‌شود؛ پس از fulfilled/dismissed دوباره آزاد است
  (ردیف قبلی فعال می‌شود، رکورد تکراری ساخته نمی‌شود).
- سقف فراخوان: ۱۰ پیام در ساعت برای هر مالک (rate_ok).
- پشتیبان JSON/زیپ دو جدول تازهٔ broadcasts/stock_requests را هم می‌برد (M2 سبز).

## درس v157 (ثبت حین بازآزمون)
- **الهام‌بخشی باگ قدیمی می‌تواند غیرمنتظره باشد:** بدهی «هم‌پوشانی .switch» در
  /my/notif-settings در واقع بیرون‌زدگی span/svg از دکمهٔ «بازگشت» بود — ریشه:
  `.notifications-hero` (flex, nowrap) دکمه را در ۳۲۰px له می‌کرد (۷۵px در برابر
  ~۱۰۵px محتوا). رفع ریشه‌ای: در موبایل `flex-wrap: wrap` + توضیح `flex:1 1 100%;
  order:3` + دکمهٔ `flex:none` (mobile-standard.css). قاعده: هر ردیف فلکسی که
  دکمه دارد، در موبایل یا wrap می‌شود یا دکمه‌اش flex:none.
- **E2E سبد:** فرم `/order/create` نام را الزامی دارد (data-validate)؛ کلیک بدون
  پرکردن نام هیچ ناوبری نمی‌کند. مسیر موفق `/order/ok?id=N` است — منتظر
  `**/order/**` عمومی ننشین.
- localStorage سبد per-context است — سبد خالی یعنی «افزودن» و «تسویه» باید در
  یک کانتکست مرورگر انجام شوند.

## ورودی کار v158 (سه بخش تازهٔ کاربر)
- صفحهٔ تنظیم اعلان‌ها برای موبایل/اندروید استاندارد نیست؛ متن‌ها روی هم انباشته
  می‌شوند → بازطراحی کامل ردیف‌های سوییچ (موبایل‌اول ۳۲۰-۴۱۲، لمس ≥44، متن ≥12).
- پس از ورود، شمارهٔ تلفن فرم کسب‌وکار = شمارهٔ ورود (کاملاً یکسان) و فیلد
  غیرقابل تغییر (readonly + اجبار سمت سرور).
- ورودی ستارهٔ نظر: کادر خط‌دار با ۶ ستاره؛ برچسب انتخاب بالای کادر
  (خیلی بد/بد/متوسط/خوب/خیلی خوب/عالی) و ستارهٔ انتخابی توپُر.

## نتیجه v158 (ثبت حین اجرا)
- ردیف‌های سوییچ اعلان‌ها به `.switch-row` (عنوان/توضیح ستونی + track انتهای ردیف)
  مهاجرت کردند؛ سلکتور حالت‌ها از `+` به `~` چون track دیگر خواهر بلافصل input
  نیست. `.switch` کهنه (booking_on و…) دست‌نخورده ماند.
- قفل تلفن: هم UI (readonly) و هم سرور (اجبار me.phone در /submit و ویرایش دکان)
  — تست سرور باید با confirm_dup برود چون seed دکان‌ها را بی‌مالک می‌سازد و
  شمارهٔ حسابِ مالک با دکانِ بی‌مالکِ هم‌شماره گیتِ تکراری را بالا می‌آورد.
- مقیاس امتیاز از ۵ به ۶: فقط فرم عوض نمی‌شود — نمایش stars()، سقف‌های سرور،
  bestRating و آستانهٔ نشان «برترین» (۹۰٪ ⇒ ۵٫۴) باید هم‌زمان بروند.
- ستارهٔ توپُر: svg آیکون fill="none" دارد؛ CSS `fill: currentColor` روی حالت
  pressed از ویژگی presentation می‌چربد — نیازی به تغییر markup آیکون نیست.
- نگهبان‌های نسخه باید فقط فایل‌هایِ تغییرکردهٔ همان انتشار را قفل کنند
  (pages.css?v158 اما mobile-standard.css?v157).

## v159 — درس‌های بستهٔ استقرار
- نسخه‌های انتشار: کد سایت تغییر نکرد؛ فقط VERSION.txt/sw.js بامپ خورد تا
  نگهبان‌ها سبز بمانند — deploy/ کدِ runtime نیست.
- backup.sh با python3 sqlite3 backup-API (بدون نصب sqlite3 CLI)؛ مسیرهای
  آپلود واقعی: site/assets/img/uploads + data/chat-files (نه پوشهٔ کهنهٔ /uploads).
- systemd: ReadWritePaths فقط data/ و پوشهٔ آپلودها؛ run.py با --port 8080
  (webbrowser در try/except است — headless امن).
- **آلودگی حالت سوئیت‌ها:** چون سوئیت‌ها مستقیم db می‌نویسند، رانِ نیمه‌کاره
  (مثلاً روی سرور مرده) ردیف آزمونی جامانده می‌گذارد و ران بعدی را کور می‌کند
  (پلن همنامِ فعالِ جامانده ⇒ چک «حذف از تعرفه» شکست می‌خورد). سوئیت‌هایی که
  رکورد می‌سازند باید ابتدای ران خودپاک‌سازی کنند (test_v3_owner اکنون می‌کند).
- playwright و مرورگرش بین نوبت‌ها از ماشین پاک می‌شوند — قبل از سوئیت‌های
  مرورگری: pip install playwright && playwright install chromium.

## تحویل تدریجی (قانون تازهٔ ۲۰۲۶-۰۹-۰۶ — خواست صریح کاربر)
- **پس از پایان هر بخش/کار، فایل نهایی همان لحظه تحویل می‌شود** — یعنی زیپِ
  کاملِ به‌روز ساخته و با present_file جلوی کاربر باز می‌شود؛ منتظر پایانِ دور
  نمی‌مانیم.
- در طول یک دور، شمارهٔ نسخه یکی است (اکنون v177)؛ هر تحویلِ میانی زیپِ همان
  دور را از نو می‌سازد (md5 تازه در KNOWLEDGE.md ثبت می‌شود) و در پایان دور،
  رگرسیون کامل + بامپ نهایی انجام می‌شود.

## مجوز ارتقای استاندارد (خواست کاربر ۲۰۲۶-۰۹-۰۶ — دور v177)
- اگر بخشی که ساخته می‌شود «نیاز به ارتقا» دارد و با تحقیق مشخص شد، ارتقای
  **استاندارد و واقعاً لازم** همان لحظه پیاده می‌شود (نه درخواست دوباره از
  کاربر، نه پرکننده) — ملاک: آنچه یک محصول استاندارد واقعی دارد، نه ویترین.
